# Environment art rollout and handoff

This is the authoritative progress tracker for the environment rollout. With this repository open, the user can say **“upgrade next zone”** in a fresh window. Follow [AGENTS.md](../AGENTS.md), resume any in-progress milestone below, or start the first planned milestone. Complete one milestone per request; do not automatically start the following one. No previous conversation or local `.qa` files are required to resume.

## Visual direction

Old Watch is the approved visual benchmark. At commit `2c2d3fe`, the user explicitly approved it: “this is great, excellent style and quality.” Carry its moderate-fidelity weathering across the island: worn masonry, aged timber, moss, natural vegetation, textured ground, readable paths and restrained atmospheric lighting. Match that quality without raising the fidelity target. Coastal settlements, jungle, volcanic terrain and the magical grove retain their own colors, vegetation, architecture and atmosphere.

Windward Farm is shipped and validated at `8b66888`, Tideglass Market / central Haven at `2e2bc1d`, Saltwind Harbor at `b8960c9`, Driftwood Yard / Sunwake Strand at `668ec8c`, Palmheart Camp / Wilds at `ac1c874`, the Cinderworks / Emberpeak at `59f9661`, and Moonwatch Observatory / Moonbloom Grove at `35c9a41`; these record implementation and QA completion, without implying separate explicit aesthetic approvals. Existing gameplay, character art, controls, objectives and UI stay intact.

**Every area keeps the style, not the colours.** What the island shares is the construction and weathering language: beveled blocks, vertex wear, metric UVs, stone footings, paired clear doors, restrained fidelity and the one lighting owner. What must change is the area's palette card — wall treatment, trim, roof colour, accent props, ground and light. Before modelling, write that card and compare it with the shipped table in the pipeline's [Area identity contract](ENVIRONMENT_PIPELINE.md#area-identity-contract): a new area may not reuse another area's wall treatment + roof colour pair, and neighbouring buildings inside one area should differ from each other as well (the harbor's three buildings use three sidings and three roofs). Open coast and beach are lighter and airier than the northern ruins; do not carry Old Watch's gloom everywhere.

## Milestones

| Order | Area | Status | Implementation / evidence |
| --- | --- | --- | --- |
| 1 | The Old Watch pilot | Complete | `2c2d3fe`; explicit user style approval; reproducible original kit. |
| 2 | Windward Farm and Old Watch connector | Complete | `8b66888`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 3 | Tideglass Market and central Haven | Complete | `2e2bc1d`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 4 | Saltwind Harbor | Complete | `b8960c9`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 5 | Driftwood Yard and Sunwake Strand | Complete | `668ec8c`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 6 | Palmheart Camp and Wilds | Complete | `ac1c874`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 7 | Cinderworks and Emberpeak | Complete | `59f9661`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 8 | Moonwatch and Moonbloom | Complete | `35c9a41`; pushed to canonical `main`, Docker deployed and verified; evidence below. |
| 9 | Island-wide landscape, shoreline, distant detail and performance | In progress — slices 1–2 of 4 complete | Coast shipped at `364e529`; shrine landmarks shipped at `5ef1079`, pushed to canonical `main`, Docker deployed and verified; slices 3–4 planned. Quiet-host timing follow-up remains open. |

## Milestone 9 plan — inventory of the retained original scenery

Measured on 2026-09-08 at `3710671` by reproducing `buildScenery`'s placement and RNG decisions (the reproduction ends on the recorded next draw `.017430383479222655`, so the counts are exact). Everything outside the eight area weights is one merged `land` batch of **116,334 triangles** plus a 510-triangle luminous batch: two draw calls for the whole island (four with shadows), so authored replacements add draw calls and renderer triangles rather than removing any. Gameplay data never changes: colliders, IDs, chests, routes, resident loops, encounter groups and the RNG stream stay as they are, and every replaced batch becomes a restorable fallback the way the areas do.

| Slice | Retained original scenery (positions in game metres) | Decision |
| --- | --- | --- |
| 1 — coast (complete) | 38 coconut palms in the beach and Haven regions: 5 collidable (`prop-2` (27, 93), `prop-3` (-35, 81), `prop-4` (31, 70), `prop-5` (-30, 60), `prop-15` (-53, 77)) and 33 decorative, 12 of them inside the Tideglass weight (the market's retained canopy) and 4 inside the yard's; 4 coastal rocks `prop-1` (-24, 101, the harbor rock inside Saltwind's weight), `prop-6` (20, 76), `prop-18` (-19, -16), `prop-19` (-8, -48); the two harbor fishing skiffs; the pier's level seaward end | Author a new island-wide `island` kit (`coast_palm_a/b`, `coast_rock_a/b`, `fishing_skiff_a/b`) instanced at the original positions, sizes and lean directions, routed through an `island-coast-original-scenery` fallback batch and a settlement skiff hook; lower the pier clamp to .85 m |
| 2 — shrine landmarks (complete) | The palm gate (two pillars at (-72.8, 1) and (-63.2, 1) under an 11.5 m lintel), the moon gate ring and orb at (78, 19), the Moonbloom shrine set (large mushrooms at (62, 20), (91, 23) and (67, 48); 7 crystals on the 13.7 m ring), the Emberpeak caldera crescent (18 rock-and-cone pairs with 6 crystals) and the ember shard core with its 10 rocks | Author in the same `island` kit; shrines, dais rings, objective markers and the primary routes stay |
| 3 — canopy beyond the areas | Jungle: 6 collidable hardwoods (`prop-7`, `-8`, `-10`, `-11`, `-13`, `-14`), rock `prop-12`, 23 decorative hardwoods and 11 jungle palms outside Palmheart's weight; moon: 5 collidable silver trees (`prop-27`, `-29`, `-30`, `-31`, `-32`), 11 decorative trees and 21 mushrooms outside Moonwatch's weight; volcano: 6 collidable rocks (`prop-20`, `-21`, `-22`, `-24`, `-25`, `-26`), 34 pebbles and 11 crystals outside the Cinderworks weight | Route into fallbacks and instance the already-resident Palmheart, Moonwatch and Cinderworks prefabs from the shared cache (no new geometry); dress collidables inside their recorded radii |
| 4 — ground cover and distance | 301 small plant clumps outside every weight (Haven 34, beach 25, volcano 113, jungle 73, moon 56), 11 shoreline flower clumps, the 20 beacon ring stones, the lighthouse (a 31 m landmark whose lantern drives the finale beam) and the shoreline seen from the ship and glider | Ground cover through the shared fern, grass, cinder and moonbell prefabs in 16 m cells with the market's distance rules; the lighthouse stays unless a later request asks (finale-facing); measure the shoreline from the ship views before deciding |

Baseline views for every slice were captured from the deployed `35c9a41` with the fixed five-player fixture (see the pipeline QA record for the camera table): the strand inland, east and west, the harbor rock and skiffs, the pier end, the yard's east palms, the beach rock, the market and Haven palms, both Haven rocks, the east Haven palms, the palm gate, the moon gate, the caldera, the wilds edge, the ship approach, the coast and Haven aerials, the strand glide and the low-quality strand. The four five-player fixtures (strand 604 / 571 calls, harbor 631 / 603, Haven 404 / 354, Old Watch 155 / 154 at high / low) all ran at 16.67 ms mean and 16.8 ms p95.

## Active handoff

- **Active milestone:** 9 — slices 1 and 2 are complete; shrine landmarks shipped at `5ef1079` on 2026-09-09. No implementation package remains active. Fable approved contract v2 before three Opus packages. Final kit: 17 roots, 16,912 source triangles, 45 primitives, 1,484,528 bytes, 50 landmark placements / 35 meshes. The six coast roots and original scenery fingerprint/RNG are unchanged. Independent regeneration is byte-identical. Lead passed **24/24 targeted and 283/283 full-suite tests**, syntax checks and `git diff --check`; inspected all 23 final working-tree views and live/failure screenshots; verified five-client browser traversal/activation, low/reduced motion, populated rendering and deliberate island/shared asset failures. Fourteen deployed views and all three fresh-context load cases passed and were visually inspected. Pillar bounds, narrow fissures and the broad shallow core were corrected without loosening contracts; see the [slice-2 evidence](ENVIRONMENT_PIPELINE.md#island-shrine-landmarks-milestone-9-slice-2-qa--2026-09-09). Quiet-host frame timing remains outstanding, not passed.
- **Latest shipped environment implementation:** `5ef1079` on canonical GitHub `main`, fast-forwarded from `e8fc3c9`; no concurrent remote commits landed during slice 2. The follow-up tracking commit changes documentation only.
- **Last confirmed deployment:** Local Docker game at `http://localhost:3400`, rebuilt from a clean `git archive` export of `5ef1079` on 2026-09-09 with `docker compose -p skywake-isles up -d --build --wait`. Service healthy, `/health` returned `ok: true`, and all **17** checked served files (eight client/shared modules and all nine kit GLBs) matched committed source by SHA-256 after CRLF normalization. Deployed browser checks repeated the three shrines, close fissures, core overhead/back, glide, low quality and Old Watch/coast comparisons, with empty console/page/network error lists; deliberately blocked island/shared GLBs restored the expected original scenery and skiffs. The `skywake-isles_skywake-data` volume remains mounted read-write at `/app/data`. Recheck current reality before working.
- **Next objective:** On the next request, milestone 9, slice 3 — canopy beyond the areas, using already-resident Palmheart, Moonwatch and Cinderworks prefabs at original placements under the inventory above. Retain original gameplay radii, procedural draws, restorable fallbacks, the six coast and eleven shrine roots, and distinct jungle/lunar/volcanic palettes. Ground cover and distance work remain slice 4. Do not start either automatically.
- **Known limitations:** Quiet-host frame-time comparisons for slices 1 and 2 remain outstanding, not failed; an unrelated game occupied CPU/GPU resources, so only renderer counts are retained. Follow the tracked camera, population and warm-sampling procedure in the pipeline's slice-2 QA section; no local `.qa` file is required. Never sample frame times during tests, Blender or another GPU workload, and do not stop the user's unrelated applications. No production blocker is recorded. Browser activation QA used controlled repositioning/guard removal for effect checks; the independent full suite supplied the actual combat voyage. Neither implementation QA nor shipping implies separate explicit user aesthetic approval.
- **Next action:** Stop after slice 2. When asked to continue, inspect repository/remote/deployment state, read the slice-3 inventory and current source, capture matching baseline views and write its bounded placement/material contracts before dispatch. This checkout may be shared: preserve unrelated `.claude/` and concurrent edits, stage explicit paths, never stash while workers write, and integrate remote history without force. Deploy clean committed archives with `-p skywake-isles`, preserving `skywake-isles_skywake-data`. Never add scene lights to a kit.

When work is interrupted, replace the active handoff with concrete current facts:

| Handoff field | Record while a milestone is active |
| --- | --- |
| Scope and status | Milestone, bounded objective, in progress / blocked / awaiting a specific check. |
| Source state | Branch/worktree, implementation commits, relevant uncommitted files, unrelated work to preserve. |
| Completed work | Installed assets/runtime changes and checks that actually passed. |
| Remaining work | Exact next action, checks yet to run, failed checks, visual/gameplay findings and unresolved decisions. |
| Evidence | Reproducible commands, tracked manifests, comparison context and measurements; summarize any local-only screenshots. |
| Publication state | Last pushed commit, last verified deployed commit, health/hash results and any remaining failure. |

Keep this handoff useful without `.qa`: local helpers may accelerate verification if inspected first, but their absence must not hide requirements or block resumption. Do not mark a milestone complete before its implementation, checks, push and deployment are verified.

## Read first

Read [ENVIRONMENT_PIPELINE.md](ENVIRONMENT_PIPELINE.md) for the detailed asset contract, generation commands, ownership, terrain/lighting constraints, validation methods and measured limitations. Also read [CREDITS.md](../CREDITS.md) and the relevant existing tests before extending the kit.

- **Shared gameplay/layout:** `shared/exploration.js`, `shared/world.js`, `shared/old-watch.js`, `shared/windward-farm.js`, `shared/tideglass-market.js`, `shared/saltwind-harbor.js`, `shared/driftwood-yard.js`, `shared/palmheart-camp.js`, `shared/cinderworks.js`, `shared/moonwatch.js`, `shared/island.js`. Use the actual target buildings, trails, resident routes, doors and loot.
- **Runtime integration:** `client/world.js`, `client/settlement.js`, `client/environment-assets.js`, `client/environment-lighting.js`, `client/environment-geometry.js`, `client/old-watch.js`, `client/windward-farm.js`, `client/tideglass-market.js`, `client/saltwind-harbor.js`, `client/driftwood-yard.js`, `client/palmheart-camp.js`, `client/cinderworks.js`, `client/moonwatch.js`, `client/island.js`.
- **Editable assets:** `tools/environment_kit.py`, `tools/build-old-watch.py`, `tools/build-windward-farm.py`, `tools/build-tideglass-market.py`, `tools/build-saltwind-harbor.py`, `tools/build-driftwood-yard.py`, `tools/build-palmheart-camp.py`, `tools/build-cinderworks.py`, `tools/build-moonwatch.py`, `tools/build-island.py`, and each kit's tracked manifest in `client/assets/`.
- **Regression coverage:** `test/old-watch.test.js`, `test/windward-farm.test.js`, `test/tideglass-market.test.js`, `test/saltwind-harbor.test.js`, `test/driftwood-yard.test.js`, `test/palmheart-camp.test.js`, `test/cinderworks.test.js`, `test/moonwatch.test.js`, `test/island.test.js`, `test/island-landmarks.test.js`, `test/environment.test.js`, plus target-area gameplay tests.

## One-milestone resume workflow

1. Read this tracker and repository instructions. Inspect the working tree, branch, recent commits and canonical remote history; preserve unrelated changes and later commits. Check Docker's current service, `/health` and deployed source before assuming the recorded deployment is still current.
2. Resume an in-progress milestone first. Otherwise use the first planned row, unless the user's current request changes the scope. Update the active handoff with the chosen objective and source state.
3. Capture the target area's existing walking, close and aerial views and a finished-area comparison. Record repeatable camera/quality/population settings and a warm performance baseline. State a concise implementation plan.
4. Implement only that milestone and the shared infrastructure it actually needs. Preserve existing IDs, append any necessary new IDs, use shared collision/layout data, keep routes/chests/both doorway approaches clear, and preserve the original procedural RNG stream. Overlays must match the alternating 2m rendered terrain triangles. Reuse the shared asset cache and single lighting owner; preserve visible fallback, atomic installation and exact-once resource disposal. Follow the pipeline's detailed constraints.
5. Complete the quality gates below, review the diff, then commit, push canonical `main` without force, deploy with `docker compose up -d --build --wait`, and verify service/health/served hashes under [AGENTS.md](../AGENTS.md). Routine completion is already authorized. Preserve `skywake-data` and other projects' containers.
6. Update this tracker with actual checks, implementation commit, push/deployment evidence, remaining limitations and the next handoff. Include the final tracking update in the authorized commit/push/deployment workflow. Report the result and stop; leave the next milestone planned.

## Quality gates and evidence to retain

- Compare against Old Watch at walking distance, close range and aerial/glider approach. Inspect interiors, animated parts, structural joins, terrain seams, path readability, vegetation placement and transition boundaries.
- Compare the area's palette card against every shipped row of the pipeline's identity table, in the running game rather than the offline preview: the shared textures are dark and the engine's light is different from Cycles, so judge brightness and hue only in the actual third-person camera next to an already-finished kit.
- Verify ordinary multiplayer movement/interactions and collision, both doors, cutaway/restoration, low graphics, reduced motion and deliberate asset-load failure. Check console and network failures.
- Regenerate assets in independent background Blender processes, preserving the user's interactive scene. Verify exported bounds/attributes/material bindings/manifests and byte reproducibility; commit original generators, assets, manifests and provenance together. Retain outward-winding and shared-resource regressions.
- Track unique download bytes, estimated decoded texture memory, visible renderer triangles/draw calls and comparable warm frame times. Include a populated gameplay view and its limitations. Per-kit ceilings do not allocate that cost to every later area; retain instancing, bounded batches and distance/quality detail reductions.
- Run relevant checks, full `npm.cmd test` for gameplay/network/rendering changes, and `git diff --check`. Record actual results. Verify pushed commit, `docker compose ps`, `/health`, and changed client assets served from the URL root against committed source.
- Keep the concise durable evidence here and detailed methods/measurements in the pipeline. Never commit credentials, saves, dependencies, previews, logs or QA output.

## Latest completed milestone evidence

**Island shrine landmarks (milestone 9, slice 2) — `5ef1079`, 2026-09-09:** committed and pushed to canonical `main`, then deployed from a clean committed archive with the existing Compose project and persistent data volume preserved. Docker reported healthy and `/health` returned `ok: true`; **17/17** served module/GLB hashes matched the commit. Fourteen deployed browser views repeated the corrected gate/ring/caldera/core, glider, low-quality and finished-area comparisons; three fresh-context load cases verified ready, island-only failure and shared-library failure. All passed, with only intentionally aborted requests failing. The final tracking update is documentation-only; implementation remains `5ef1079`.

The slice adds eleven authored roots at fifty original sites: two jungle pillars and a lintel; lunar ring, suspended orb, three oversized mushrooms and seven crystal clusters; eighteen caldera ridges, six amber clusters, a broad shallow ember core and ten rocks. Full position/XYZ rotation/nonuniform scale and original procedural draws are preserved. Repeated landmarks use static 64m cells and shared bound materials; landmarks remain on low graphics and at range. Both original island fallback parents and skiffs restore atomically. No collider, gameplay data, charge ring, shrine dais, route, chest, resident, encounter, terrain, light or six-root coast geometry changed. Canopy and ground cover were not started.

Lead independently passed **24/24 targeted and 283/283 full-suite tests** and byte-identical regeneration. The final island GLB is **1,484,528 bytes / 16,912 triangles / 45 primitives**, with no added textures; unique environment GLBs total **27,248,188 bytes** and decoded texture memory remains **9,786,696 bytes**. The working-tree 23-view review and five-client actual-browser input checks covered traversal, activation, charge/completion effects, low/reduced motion and populated rendering; controlled repositioning and guard removal in that fixture are documented, while the full suite supplies aimed-fire combat voyage and persistence coverage. Renderer counts rose by 12 / 27 / 16 calls in the Palm / Moon / caldera fixtures. Quiet-host frame times remain outstanding for slices 1–2; no performance-pass claim is made from contaminated samples. Full hashes, cameras, counts and limitations are in the [pipeline QA record](ENVIRONMENT_PIPELINE.md#island-shrine-landmarks-milestone-9-slice-2-qa--2026-09-09). This records implementation/QA completion, not additional explicit user aesthetic approval. Slices 3–4 remain planned.

### Previous slice

**Island coast (milestone 9, slice 1) — `364e529`, 2026-09-08:** committed and pushed to canonical `main` after rebasing onto the concurrent airship commit `2656d45`; Docker was rebuilt from a clean `git archive` export of that commit with `docker compose -p skywake-isles up -d --build --wait`, `docker compose ps` reported healthy, and `/health` returned `ok: true`. All **29** checked served browser assets — the five changed client modules, the new shared module, the airship commit's nine client and three shared modules, three unchanged shared layout modules and all nine kit GLBs — matched the committed source by SHA-256 after CRLF normalization. The `skywake-isles_skywake-data` volume mount was unchanged. Full `npm.cmd test` passed **245 tests** on the slice-1 tree and **275** on the rebased head (six coast regressions added; thirty airship tests arrived with the concurrent commit); `git diff --check` passed. The island kit regenerated byte for byte in two independent Blender runs (GLB `47274b38…7a8d`, manifest `8804c1af…fdcb`; the tint revision changed only the manifest); the eight earlier kits are unchanged.

The slice replaces the 38 original cone-and-frond palms of the beach and Haven regions (five collidable, twelve of them the market's retained canopy) with leaning sun-bleached coconut palms at the original positions, sizes and lean directions, dresses the four coastal rocks (`prop-1`, `prop-6`, `prop-18`, `prop-19`) with pale surf-worn boulders scaled to their recorded radius and height, replaces the two harbor skiffs with clinker boats parented to the original bobbing pivots, and lowers the strand pier's seaward clamp to .85 m. No obstacle, chest, route, resident loop or random draw changed; the original palms, rocks and skiffs remain as the kit's fallback. The geometry adds **906,160 GLB bytes** (10,424 source triangles, 20 primitives) and no texture images; combined unique environment GLBs total **26,669,820 bytes**, retaining **9,786,696 bytes** estimated decoded texture memory. The kit adds about 43–46 draw calls and 63–69k renderer triangles to the strand and harbor fixtures (604 → 647 and 631 → 677 calls at high quality), 17 calls at the Haven and nothing at Old Watch; the frame-time comparison could not be sampled on a quiet host this session (see the known blocker) and is the one outstanding check; details in [the pipeline QA record](ENVIRONMENT_PIPELINE.md#island-coast-milestone-9-slice-1-qa--2026-09-08).

Validated the strand inland, east and west, the harbor rock and skiffs, the pier end, the yard's east palms, the beach rock, the market and Haven palms, both Haven rocks, the east Haven palms, the Old Watch comparison, the ship approach, both aerials, the glider and the low-quality strand with a clean console and network log, from the working tree and again from the deployed container. Browser failure checks on both servers: blocking the island kit restored the original palms, rocks and skiffs while the eight area kits stayed ready; blocking the shared library restored every area; a 404 before the generator finished took the same path. Visual review confirmed ten of the eleven tints derived from the measured texture means at first sight and revised one, the surf stone, from a grey-green 4.0/3.6/3.0 to a warm 5.2/4.2/3.4 without touching the GLB. Slices 2–4 remain planned.

### Previous milestone

**Moonwatch Observatory and Moonbloom Grove — `35c9a41`, 2026-09-07:** committed and pushed to canonical `main` after fast-forwarding onto the concurrent reload-animation commit `99750cd` (no other commits landed during the milestone); Docker was rebuilt from a clean `git archive` export of that commit with `docker compose -p skywake-isles up -d --build --wait`, `docker compose ps` reported healthy, and `/health` returned `ok: true`. All **25** checked served browser assets — every changed client module, the concurrent commit's two client modules, the new shared module, the unchanged shared layout modules and all eight kit GLBs — matched `git show HEAD:<path>` by SHA-256 after CRLF normalization. The `skywake-isles_skywake-data` volume mount was unchanged. Full `npm.cmd test` passed **239 tests** (eleven observatory regressions added; six reload-animation tests arrived with the concurrent commit); `git diff --check` passed. The observatory kit regenerated byte for byte in two independent Blender runs (GLB `3453b31b…8d78f7`, manifest `b9d45d3a…a4bad`; the tint revision changed only the manifest); the seven earlier kits are unchanged.

The milestone replaces the flat observatory drum with a non-enterable silvered stave drum on a cut-ashlar plinth under a ribbed moon-glass dome with verdigris bronze ribs, a closed slit hatch and a brass star orb, a closed pale door up three ashlar steps and two portholes; it replaces the telescope, star-table, chart-crate and lantern sites with a brass telescope on a pale tripod, an inlaid ashlar star table, a chart crate with rolled scrolls and a moon-glass lantern, adds an armillary sphere and a scholar's bench by clearance, dresses the collidable tree at `(109, 35)` and rock at `(109, 57)` inside their recorded radii, and plants 22 curling silver trees, 24 luminous mushrooms, 26 moon crystals, 110 moonbell clumps and 180 lilac-tinted ferns and grass clumps over a lilac moss-and-gravel overlay, under a cool luminous lighting profile whose only glow is the emissive glass binding. No obstacle, chest or building IDs changed; Lio still walks the loop and the guards still spawn at `(97, 31)`. The geometry adds **2,444,180 GLB bytes** (26,784 source triangles, 43 primitives) and no texture images; combined unique environment GLBs total **25,763,660 bytes**, retaining **9,786,696 bytes** estimated decoded texture memory. Five-player fixed views stayed vsync-bound (**17.5–17.6 ms mean / 18.1–18.3 ms p95 before and 16.67 / 16.8 ms after on the same host**, the spread being host scheduling), with the dome square rising **100 → 165 calls / 294k → 526k triangles**, the trail **119 → 203 / 304k → 558k**, the grove edge **124 → 208 / 305k → 559k** and the Old Watch comparison unchanged at 150 calls; details in [the pipeline QA record](ENVIRONMENT_PIPELINE.md#moonwatch-observatory-and-moonbloom-grove-milestone-qa--2026-09-07).

Validated walking views of the dome square, front, side and back, the telescope, star table, chart crate, lantern and chest corner, the trail approach, the grove edge, the dressed silver tree and moon boulder, both aerials, the glider approach, the low-quality dome and the Old Watch comparison with a clean console and network log, from the working tree and again from the deployed container. Browser failure checks on both servers: blocking the observatory kit restored the original dome, work sites and grove scenery with the baseline sky while the seven other kits stayed ready; blocking the shared library restored every area. Visual review replaced dead-olive leaves, brown bark, warm tan staves, dark plum mushroom caps and green moonbells (tints chosen before the shared textures were measured) with lavender, silver and lilac values derived from the measured texture means, shifted the moss overlay from grey to lilac, and re-anchored the scholar's bench from the telescope corner to the star table; a first frame-time run that overlapped a concurrent test run read 83 ms and was discarded.

### Earlier milestone

**Cinderworks and Emberpeak — `59f9661`, 2026-09-07:** committed and pushed to canonical `main` (no concurrent commits landed during the milestone); Docker was rebuilt from a clean `git archive` export of that commit with `docker compose -p skywake-isles up -d --build --wait`, `docker compose ps` reported healthy, and `/health` returned `ok: true`. All **17** checked served browser assets — every changed client module, the new shared module, the unchanged shared layout modules and all seven kit GLBs — matched `git show HEAD:<path>` by SHA-256 after CRLF normalization. The `skywake-isles_skywake-data` volume mount was unchanged. Full `npm.cmd test` passed **222 tests** (ten forge regressions added); `git diff --check` passed. The forge kit regenerated byte for byte (GLB `543754ec…abea`, manifest `4454e094…02e9`); the six earlier kits are unchanged.

The milestone replaces the forge's flat box with a coursed-basalt and iron-strapped smithy under lapped rust iron sheet, with iron-banded jamb posts over both open doorways, a hanging hammer sign, a vent grille on each side, a tool board, an interior flue hood over the bench, a basalt flagstone floor and a chimney stack seated on the original smoke emitter; it replaces the anvil, ore-pile and lantern sites with an anvil on a basalt block with a quench trough, heaped ore with ingots and a strapped crate, and an iron lantern post, adds a coal bin and a slag heap by clearance, dresses the collidable rock at `(69, -34)` with a columnar basalt outcrop inside its radius, and plants 16 basalt boulders, 120 cinder clumps, 22 ember crystals and 90 scorched grass clumps over an ash overlay with soot rings and scorched tracks, under a warm ash-haze lighting profile. No obstacle, chest or building IDs changed; Sula still walks her loop and the guards still spawn at `(80, -55)`. The geometry adds **2,768,008 GLB bytes** (30,284 source triangles, 56 primitives) and no texture images; combined unique environment GLBs total **23,319,480 bytes**, retaining **9,786,696 bytes** estimated decoded texture memory. Five-player fixed views stayed vsync-bound (**16.67 ms mean / 16.8–16.9 ms p95** before and after on the same host), with the forge square rising **155 → 226 calls / 306k → 377k triangles**, the trail approach **113 → 193 / 294k → 376k**, the caldera edge **269 → 283 / 641k → 656k** and the Old Watch comparison unchanged at 150 calls; details in [the pipeline QA record](ENVIRONMENT_PIPELINE.md#cinderworks-and-emberpeak-milestone-qa--2026-09-07).

Validated walking views of the forge square, front, chimney side and back, the interior cutaway, the anvil, ore pile, lantern and chest corner, the trail approach, the outcrop, the caldera edge, the shrine-to-forge boundary, both aerials, the glider approach, the low-quality forge and the Old Watch comparison with a clean console and network log, from the working tree and again from the deployed container. Browser failure checks on both servers: blocking the forge kit restored the original forge, work sites and Emberpeak scenery with the chimney smoke and baseline sky while the six other kits stayed ready; blocking the shared library restored every area. Visual review replaced black-silhouette basalt (a .70 tint over .45 vertex colours), a hard dark ash disc and a grey overcast sky with basalt near 1.75, an overlay that fades with the square of the weight and a warmer, brighter profile; the lighting ramps were widened to 9 m so no step exceeds .01 per .1 m. Milestone 8 remains planned.
