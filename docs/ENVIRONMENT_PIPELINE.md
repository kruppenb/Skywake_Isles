# Skywake Isles environment pipeline

Old Watch is the first playable environment sample for a weathered northern coastal art direction. The kit uses original mossy gray-green masonry, silvered timber, dark overlapping slate, irregular fir boughs, tawny grass and pinnate ferns. Warm lantern glass provides a small point of contrast. It is an original style study, with no assets copied from Skyrim or another game.

Windward Farm and its Old Watch connector are the second area. They carry that approved material quality into a working farm: aged limewash over stone, warm barn timber, muted russet roofing, turning framed sails, golden crops, leafy vegetables and worn field edges. The visual benchmark remains Old Watch; later regions should retain their own vegetation, architecture and atmosphere at this fidelity.

Tideglass Market and immediate central Haven are the third area: pale weathered plaster, aged timber, teal tile roofs, coral and teal canvas, market stock, sandy walking routes and restrained broadleaf planting. The existing market cottage, two stalls and two closed Haven huts receive authored geometry. Original palms, residents, furniture, lighthouse, objectives and gameplay layout remain intact.

Saltwind Harbor is the fourth area and the first of the coastal working waterfront. Its three enterable buildings share the kit's construction language but deliberately not its colours: the net-house is unpainted salt-grey board-and-batten with tarred trim under green cedar shakes; the tavern is lime-washed cream clapboard with a honey-oak frame, terracotta shingles, a fieldstone chimney, a hanging fish sign and a door lantern; the fisher's cottage is whitewashed shiplap with sea-blue louvred shutters under sun-faded salmon tiles. Authored work sites (mending table, crates and barrel, drying net, lantern post), mooring pilings, lobster pots, pale sand with shell-grit routes and straw dune grass replace the original flat props and lawn tufts. Boats, residents, furnishings, loot, the Sunwake landing and gameplay layout remain intact.

Driftwood Yard and Sunwake Strand are the fifth area, served by one kit. The yard is raw boatyard timber and pitch: the timber shed is fresh-sawn honey weatherboard with pitch-black tarred posts, sills and door frame under tarred roof boards with bleached cover battens, a half-hull sign over its front door and a tool rack on its back wall; the shipwright's cottage is oxblood-painted clinker with bleached driftwood-white corner boards, solid shutters, oars by the door and a carved gull finial over silver-grey wooden shingles. The original hull, timber stack and lantern sites receive an open-ribbed hull frame on cradle blocks, stacked baulks and a squared lantern post; a sawhorse bench, a pitch kettle and a second stack are placed with clearance, over a sawdust-and-worked-sand overlay. The strand is bleached wood and open sand: a terrain-following pier of five deck sections on tarred pilings, two banner poles carrying a rope of sailcloth pennants that sway in the wind, rope-tied landing crates at the two original crate positions, a fingerboard signpost, storm-wrack driftwood logs, sea-oat grass and a near-white sand overlay with a damp band toward the water. The drop point, routes, Ada's loop, the supplies, chests and the guarded inland approach stay clear.

Palmheart Camp and the immediate Palmheart Wilds are the sixth area and the first inland jungle kit. The camp is woven palm and lashed hardwood rather than sawn timber: the trailkeepers' tent is an A-frame of lashed dark hardwood poles under layered palm-frond thatch, with woven-mat gables, a rolled canvas door flap (the tent stays non-enterable) and a buried packed-earth skirt; the original fire ring, crate-and-flag and lantern sites receive a sooted fieldstone ring with charred logs, an iron tripod and kettle and split-log benches, a lashed drying rack with hung gear, a slatted crate, a water cask and a coral trail-keeper pennant, and a bark post with a thatched hood over an amber lantern. The wilds vegetation is authored rather than borrowed: buttressed broadleaf hardwoods with modelled ovate leaves, tall ringed jungle palms with pinnate fronds, elephant-ear ground clumps and the shared ferns and grass, over a humus-and-leaf-litter overlay with a trodden path to the shrine. Nothing collides: the trees are walkable decorations like the original jungle canopy, and the existing collidable jungle trees inside the area only get authored trunks inside their recorded radii, because the camp's seven guards spawn around `(-88, 12)` from those very obstacles. The shrine, its objective ring, the primary route, Bram's loop, chest-23 and the encounter group stay clear.

The Cinderworks and the immediate Emberpeak apron are the seventh area and the first volcanic kit. The forge is scorched basalt and iron rather than timber or plaster: coursed black basalt blocks to 2.25 m with a soot-stone backing, oiled charred vertical boards bound by riveted iron straps above, iron-banded jamb posts and a strapped header over both open doorways, a narrow iron-grilled vent slit on each side wall, a hanging hammer sign on chains, a tool board on the back wall, and a low-pitched roof of lapped rust-red iron sheets with rivet rows under a basalt-and-soot chimney stack and iron cowl seated on the fallback's smoke emitter. Inside, a basalt flagstone floor and an iron-and-basalt flue hood over the smith's bench keep the retained workbench, casks and chest. The original anvil, ore-pile and lantern sites receive an anvil on a basalt block with a quench trough and tools, heaped ore with ingots and a strapped crate, and an iron lantern post; a coal bin and a glassy slag heap are placed by clearance. The apron is ash and cinder: a columnar basalt outcrop dresses the collidable rock at `(69, -34)` inside its recorded radius, basalt boulders, scoria cinder clumps, amber ember crystals and ochre scorched grass sit over a black ash overlay with soot rings at the work sites and scorched tracks along the walked routes. The caldera crescent, the ember shard, its shrine and ring, the primary route, the trail, Sula's loop, chest-24, chest-14 and the encounter group stay clear; the volcano's terrain tint and ember motes are untouched.

Moonwatch Observatory and the immediate Moonbloom Grove are the eighth area and the first night-lit kit. The dome is a non-enterable silvered stave drum on an ashlar plinth: pale silvered vertical staves bound by three pale wood hoops rise from a plinth of cut moon-grey ashlar up three ashlar steps to a closed pale door, past two round portholes, under a ribbed moon-glass dome with verdigris bronze ribs, a closed observation-slit hatch and a brass star orb on its finial. The four original work sites are replaced by a brass telescope on a pale tripod, an inlaid ashlar star table with a bronze ring and gnomon, a chart crate with rolled scrolls, and a moon-glass lantern; an armillary sphere and a scholar's bench are placed by clearance. The Moonbloom grove around it is authored rather than borrowed: curling silver trees with lavender leaf clusters and hanging moon-glass motes, luminous moon mushrooms, moon crystals, moonbell clumps and the shared ferns and grass tinted lilac, over a silver-lilac moss and pale gravel overlay; the collidable tree and boulder inside the weight receive only an authored silhouette, never a new collider. The Moonbloom Shrine, its crystal ring, the stone gate, the primary route, the trail, Lio's loop, chest-25, the encounter group and the moon motes stay clear.

## Build and inspect

The editable sources are `tools/build-old-watch.py`, `tools/build-windward-farm.py`, `tools/build-tideglass-market.py`, `tools/build-saltwind-harbor.py`, `tools/build-driftwood-yard.py`, `tools/build-palmheart-camp.py`, `tools/build-cinderworks.py` and `tools/build-moonwatch.py`. Shared deterministic mesh construction, metric UVs, vertex colors, material creation, tileable-noise helpers and glTF export live in `tools/environment_kit.py`. Each area retains its explicit models, seed, layout contract and exported bounds validation. Old Watch exports the self-contained shared library; Farm, Tideglass and Saltwind export uncompressed glTF 2 geometry extensions. Saltwind's generator derives every building's envelope from the same `radius`/`height` formula as `shared/exploration.js` and builds all three from one parametrized shed builder whose siding, trim, roof and extras come from a per-building spec, so a new building is a spec entry plus any signature detail, not a copy of another building's code. The generators need Blender 5.2 or a compatible newer Blender with its bundled NumPy and glTF exporter; they do not need downloaded assets, image libraries, an asset CDN, or a running game server.

From the repository root in PowerShell:

```powershell
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-old-watch.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-windward-farm.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-tideglass-market.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-saltwind-harbor.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-driftwood-yard.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-palmheart-camp.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-cinderworks.py
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-moonwatch.py
```

Use a different Blender executable path where appropriate. `--background --factory-startup` starts an independent process and never touches an artist's open Blender scene. To render an optional inspection image outside the shipping asset directory:

```powershell
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-old-watch.py -- --preview .qa/old-watch-kit-preview.png
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-windward-farm.py -- --preview .qa/farm-kit-preview.png
```

To compare a regeneration without overwriting shipping output:

```powershell
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-old-watch.py -- --output .qa/old-watch-regenerated
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-windward-farm.py -- --output .qa/farm-regenerated
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-tideglass-market.py -- --output .qa/tideglass-regenerated
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-saltwind-harbor.py -- --output .qa/saltwind-regenerated --preview .qa/saltwind-preview.png
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-driftwood-yard.py -- --output .qa/driftwood-regenerated --preview .qa/driftwood-preview.png
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-palmheart-camp.py -- --output .qa/palmheart-regenerated --preview .qa/palmheart-preview.png
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-cinderworks.py -- --output .qa/cinderworks-regenerated --preview .qa/cinderworks-preview.png
& 'C:/Apps/Blender/blender.exe' --background --factory-startup --python-exit-code 1 --python tools/build-moonwatch.py -- --output .qa/moonwatch-regenerated --preview .qa/moonwatch-preview.png
```

Each seed is fixed. Keep the Blender/exporter version fixed when comparing binary hashes; exporter versions can change the binary representation. `--python-exit-code 1` makes validation errors fail the CLI process. Commit the source generator, area's `client/assets/<area>/kit.glb` and regenerated `manifest.json` together. Keep previews, build logs, temporary textures and Python cache files out of commits. All kits load from `/assets/<area>/kit.glb` on the same local server as the game. Farm and Tideglass accept `--preview <ignored-output.png>` and bind generated shared materials only after export, so preview textures never enter their shipping GLBs.

Helper extraction preserved the approved Old Watch binary and manifest byte for byte under Blender 5.2.1 LTS. Its GLB SHA-256 remains `3fce2c1f50178bf0a681fdec6aafba86b98140cc108ce6ae522b45e6370f152b`. Independent Farm regeneration also matches its shipping GLB and manifest; use each manifest's hash as the authoritative asset version.

## Asset contract

Units are meters. Authoring helpers accept game coordinates: X right, Y up, +Z forward. They convert to Blender's Z-up coordinates before exporting Y-up glTF. Every prefab root and its children have identity transforms, with geometry already in the building or prop's ground-centered frame. Front doors face +Z. The runtime supplies position, yaw and instance scale.

| Root nodes | Purpose and envelope |
| --- | --- |
| `watch_tower` | Solid tower, radius under 3m, height under 10m; closed arched door, slit recesses, buttress feet, worn parapet and warm lantern. |
| `barracks_base` | 4.26 × 3.54m floor and low stone perimeter; two clear centered 2.30m doors. |
| `barracks_wall_east`, `barracks_wall_west` | Individually removable long walls, up to 3.20m; timber infill, stone base and recessed leaded windows. |
| `barracks_wall_front`, `barracks_wall_back` | Individually removable end walls; 2.30m clear width below the 2.80m header. |
| `barracks_roof` | Separate gable/rafters/slate assembly, under 4.60 × 3.90m and 5.50m total height. |
| `ruin_wall` | Broken mossy wall, approximately 4.20 × 0.65 × 1.95m including the buried base. |
| `rock_a`, `rock_b` | Two shaped boulders within 1m horizontal radius, from -0.25m to at most 1.25m. |
| `pine_a`, `pine_b` | 8m / 10m trees, 0.28m / 0.32m nominal trunk radii; irregular modeled boughs rather than cone stacks. |
| `grass_clump`, `fern_clump` | Curved tapered grass and arching paired fern leaflets; merged geometry for instancing. |
| `ground_sample` | Material carrier for `ground_earth`; hidden by the runtime, which reuses its textures for the terrain overlay. |

Each prefab contains one merged mesh per material. The separate barracks parts share the same building frame: do not recenter each face or place furniture in those roots. Existing gameplay furnishings and the existing cutaway controller are retained by the runtime.

`manifest.json` records exported bounds, radius, triangle and primitive counts, materials, texture dimensions, file size and SHA-256. The generator checks names, identity transforms, embedded resources, UVs, vertex colors, finite positions, footprint/height limits, door clearance and the hard size/triangle budgets against the exported GLB, rather than only against the source objects.

The Farm extension has these additional roots, using the same identity-transform convention:

| Root nodes | Purpose and envelope |
| --- | --- |
| `windmill_body` | Tapered worn plaster, exposed fieldstone, timber hoops, closed door and russet tiled cap; foundation to -0.65m, visible top 11.75m, within the existing 3.40m radius. |
| `windmill_sails` | Four framed, modeled sails, centered on the rotor origin; rotation in local XY, sweep under 2.48m. Mount at mill-local `(0, 7.82, 2.108)`. |
| `barn_base` | 4.97 × 4.13m floor and low masonry; floor top 0.025m, no threshold across either doorway. |
| `barn_wall_east`, `barn_wall_west`, `barn_wall_front`, `barn_wall_back` | Four complete removable faces, with braces, vents and 3.48m wall height; both centered 2.30 × 2.80m doors remain clear. |
| `barn_roof` | Separate gables, rafters, sealed roof planes and russet tiles; about 5.20 × 4.47m, under the 6m building envelope. |
| `crop_wheat`, `crop_leafy` | Seven-stalk wheat clump and modeled broad leaves; one material primitive each, heights under 0.90m / 0.50m. |
| `fence_section`, `hay_bale` | Two-meter split-rail span with buried posts and a tied, textured hay stack; original geometry. |

The Farm manifest additionally declares the shared kit dependency, named `materialBindings`, zero embedded images, zero added decoded texture memory and the rotor pivot. Export checks validate those contracts and the full paired doorway prisms. Shared rock outward winding remains protected by the signed-volume regression in `test/old-watch.test.js`, including rocks reused as flattened paving.

Tideglass uses eleven additional identity-transform roots:

| Root nodes | Purpose and envelope |
| --- | --- |
| `cottage_base` | 4.544 × 3.776m timber floor and backed stone perimeter; floor top .025m, base top .48m; paired 2.30m doors remain clear. |
| `cottage_wall_east`, `cottage_wall_west`, `cottage_wall_front`, `cottage_wall_back` | Independently removable plaster/timber faces between .48 and 3.20m, including recessed shutters and door headers above 2.80m. |
| `cottage_roof` | Sealed gable/underside, overlapping teal tiles and ridge; from 3.20 to 5.491m, within .16m overhangs. |
| `fruit_stall`, `sailcloth_stall` | Timber stock cabinets, tied double-surface sagging/scalloped canvas and distinct produce/rolled cloth. Foundation -.50m, top 3.44m, radius under the existing 2.50/2.40m colliders. |
| `haven_hut` | Closed timber door, shutters, worn plaster/stone and teal roof; foundation -.50m, top 4.841m, radius 2.962m inside the original 3.20m collider. |
| `paving_slab` | Convex outward-wound 28-triangle stone, radius under .55m, top under .035m. Runtime instances fit local terrain slopes. |
| `coastal_shrub` | Sixteen modeled broad leaves/stems, 576 triangles in one primitive, under .50m radius and .80m height. |

The Tideglass exporter clips complete triangles against both open doorway prisms, validates normals/UVs/vertex colors, identity transforms, bounds, shared dependency hash, size and zero images. Tests independently check triangle/door intersections, roof underside coverage, every paving face's outward direction and runtime building footing/height envelopes. The two hut yaws are taken after their original RNG draws; no gameplay IDs or colliders are added.

Saltwind Harbor uses twenty-four identity-transform roots:

| Root nodes | Purpose and envelope |
| --- | --- |
| `net_house_base`, `net_house_wall_east/west/front/back`, `net_house_roof` | 5.112 × 4.248m, wall 3.48m, ridge 5.85m under the 6m envelope; batten boards, tarred trim, cedar shakes, a closed hoist hatch on both gables and a hoist beam with pulley projecting .68m past the harbor-facing gable (the only allowed footprint excess). |
| `tavern_base`, `tavern_wall_*`, `tavern_roof` | 5.68 × 4.72m, wall 4.06m, ridge 6.55m; lapped clapboard courses, oak posts and header, terracotta shingles, and a fieldstone stack reaching 6.945m at the fallback's smoke emitter `(-1.647, -1.038)` so the retained plume rises from the stone. The front carries a swinging fish sign and an amber door lantern, both proud of the siding but within .16m of the footprint. |
| `fisher_cottage_base`, `fisher_cottage_wall_*`, `fisher_cottage_roof` | 4.26 × 3.54m, wall 3.20m, ridge 5.28m; wide painted shiplap, sea-blue posts, louvred shutters and two hung buoys beside the front door. |
| `drying_net`, `mending_table`, `fish_crates`, `harbor_lantern` | Replacements for the four original harbor work sites, footed at the recorded site positions and radii (1.7, 1.9, 1.3 and .55m); nets are `net_cord` tubes with cork floats and sway in the wind uniform. |
| `dock_post`, `lobster_pot` | Mooring piling with an iron cleat (radius .22m, 1.7m) and a rib-and-twine pot (radius .57m, .48m). |

All three buildings keep the shared cottage/barn wall contract: base top .48m, floor .025m, both 2.30 × 2.80m doors clear, walls capped at their `wallHeight`, roofs within .175m overhangs and the building's `height`. The exporter validates every building's door prisms from its own dimensions, the chimney height, prop radii and a 3.5MiB / 42k-triangle guard; tests recheck the doorways, roof envelopes, the chimney seat and the work-site lookup at runtime scale.

Driftwood Yard and Sunwake Strand use twenty-three identity-transform roots:

| Root nodes | Purpose and envelope |
| --- | --- |
| `timber_shed_base`, `timber_shed_wall_east/west/front/back`, `timber_shed_roof` | 4.686 × 3.894m, wall 3.48m, ridge 5.85m under the 6m envelope; horizontal waney-edge weatherboards with knots, tarred posts and sills, a half-hull sign proud of the front gable above the header, a pegged tool rack on the back wall, and vertical tarred roof boards under bleached cover battens and ridge board. |
| `shipwright_cottage_base`, `shipwright_cottage_wall_*`, `shipwright_cottage_roof` | 4.26 × 3.54m, wall 3.20m, ridge 5.28m; oxblood clinker courses with pale worn lips, bleached corner boards and frames, solid diamond-painted shutters, two oars on the front skirt ledge, staggered silver shingles, bleached barge boards and a carved gull finial. No chimney. |
| `hull_frame`, `timber_stack`, `yard_lantern` | Replacements for the three original yard work sites, footed at the recorded positions and radii (3.5, 2.0 and .55m): tarred keel, stem and stern posts, seven rib stations and fitted strakes on two cradle blocks; baulks and planks on bearers with a rope tie; a squared raw post with an iron hood and amber glass. |
| `sawhorse_bench`, `pitch_kettle` | Loose yard tools (radius 1.5 and .75m) placed by clearance beside the hull: two sawhorses, a plank, a bow-saw and mallet; an iron cauldron on legs over a fieldstone ring with a tar brush. |
| `pier_section` | One 2.4m length of the landing pier: 4.4m deck of bleached planks on tarred stringers, two tarred pilings reaching -4.0m with rope wraps and bollard tops; the runtime places five and pitches each toward its neighbours. |
| `banner_pole`, `banner_line` | 5.4m bleached poles (buried .5m) with rope wraps and an iron finial ring; a 14m sagging rope with nine single-sided sailcloth pennants coloured per pennant by vertex colour, swayed by the wind uniform. |
| `landing_crates`, `driftwood_log`, `strand_signpost` | Rope-tied crates and a sack (radius 1.15m), a gnarled bleached log (radius 1.4m, .7m high) and a post with three painted-tip fingerboards (radius .8m, 3.1m). |

Both buildings keep the shared wall contract (base top .48m, floor .025m, both 2.30 × 2.80m doors clear, walls capped at `wallHeight`, roofs within .175m overhangs and the building's `height`); hung details stay within .16m of the front and back faces, which is why the oars stand on the skirt ledge and the tools lie on a rack rather than hanging. The exporter validates both buildings' door prisms, every prop envelope above and a 3.5MiB / 40k-triangle guard; tests recheck the doorways, roof envelopes, pier ramp, pennant material and the work-site lookup at runtime scale.

Palmheart Camp uses eight identity-transform roots:

| Root nodes | Purpose and envelope |
| --- | --- |
| `trailkeepers_tent` | Non-enterable A-frame inside the tent's 3m collider: radius under 2.95m, buried skirt to -.5m, ridge under 4.1m; lashed hardwood tripods and rafters, layered frond thatch overhanging the woven-mat gables, a rolled canvas flap over a closed hanging flap on +Z, a stone step. It has no cutaway contract. |
| `fire_ring`, `gear_rack`, `camp_lantern` | Replacements for the three original camp sites, footed at the recorded positions and radii (1.7, 1.7 and .55m): sooted fieldstones around ash and charred logs with an ember block, an iron tripod and kettle, split-log benches and firewood; a lashed drying rack with a hung cloth, rope coils, a slatted crate, a water cask and a pennant pole; a bark post with rope wraps, an iron cage and a thatched hood over amber glass (2.7m). |
| `jungle_tree_a`, `jungle_tree_b` | Buttressed broadleaf hardwoods, radius under 3.4 / 3.8m and 9 / 11m tall, with modelled ovate leaf clusters and (on b) hanging lianas. Every `jungle_bark` vertex below 1m stays within .40 / .44m of the axis so a scaled trunk sits inside a recorded collider; the buttress fins are `jungle_hardwood` tinted to match. |
| `jungle_palm` | Ringed curved trunk (radius under .32m below 1m), nine pinnate fronds of paired leaflets, a skirt of dead fronds and a fruit cluster; radius under 3.3m, 9.5m tall. |
| `elephant_ear_clump` | Large heart-shaped leaves and furled shoots on arching stalks, one `elephant_ear` primitive, radius under .9m and 1.1m tall. |

Two site radii coincide (fire ring and gear rack are both 1.7m), so the runtime tells them apart by the anchor the settlement searched from rather than by radius alone. The exporter validates every envelope above, the trunk-radius contract, zero images and a 3.0MiB / 36k-triangle guard; tests recheck them from the shipping GLB and the work-site lookup at runtime.

The Cinderworks uses seventeen identity-transform roots:

| Root nodes | Purpose and envelope |
| --- | --- |
| `cinder_forge_base`, `cinder_forge_wall_east/west/front/back`, `cinder_forge_roof` | 4.828 × 4.012m, wall 4.06m, ridge 6.40m under the 7m envelope; a basalt flagstone floor at .025m and basalt perimeter to .48m; coursed basalt to 2.25m over a soot-stone backing plane that seals the joints, charred vertical boards with three riveted iron straps above, iron-bound corner posts, a vent slit with an iron grille on each side wall; masonry stops at x ±1.35 so the .20m iron-banded jamb posts own each doorway reveal, and a strapped boarded header spans 2.80m to the wall top. The west wall carries an interior flue hood from 1.95m to the wall top over the bench (never inward of x = -1.65) with a recess and ember block; the back wall a hung tool board within .16m. The roof seals both gables, lays two lapped courses of ribbed rust sheet per side with rivet rows and eave straps, and seats a basalt/soot stack with an iron cowl at the fallback's smoke emitter `(-1.400, -.883)`, top 6.90m, cowl 6.949m. |
| `forge_sign` | Hinge-origin hammer sign: two chains and a plate, all `sign_plate`, within radius .45m, y from -.75 to .03m and z from -.02 to .064m; the runtime hangs it at building-local `(1.55, 3.85, depth/2 + .09)` so it stays inside the .16m hung-detail bound and swings from its origin. |
| `forge_anvil`, `ore_pile`, `forge_lantern` | Replacements for the three original forge sites, footed at the recorded positions and radii (1.6, 1.7 and .55m): a basalt block and timber stump under an iron anvil with hammer and tongs, a banded quench trough with a dark water disc and a bucket; heaped ore lumps, three ingots, a strapped crate and scoria; a stone-footed square iron post with a bracket, cage and amber glass (2.4m). |
| `coal_bin`, `slag_heap` | Loose props placed by clearance (radius 1.0 and 1.3m): a strapped plank bin heaped with coal and a leaning shovel; a glassy scoria mound with inset amber glints. |
| `basalt_outcrop` | Nine tilted hexagonal basalt columns with uneven tops and a scoria skirt, tallest 7.98m, every vertex within radius 2.71m and y from -.48m; it dresses the collidable rock `prop-23` (radius 3, height 8) at scale 1. |
| `basalt_boulder_a`, `basalt_boulder_b` | Blocky columnar-fracture boulders, radius under .82 / .99m and 1.04 / 1.35m tall. |
| `cinder_clump`, `ember_crystal` | One-primitive scatter pieces: five to seven scoria lumps under .47m radius and .26m; three to five faceted amber shards under .32m radius and .92m. |

The exporter validates both doorway prisms, the wall/base/roof bounds, the chimney seat, the sign's depth, the outcrop's radius and height, every prop envelope, zero images and a 3.5MiB / 40k-triangle guard; tests recheck them from the shipping GLB, the chimney contract against `-width × .29 / -depth × .22`, the cutaway swap and the work-site lookup at runtime.

Moonwatch uses thirteen identity-transform roots:

| Root nodes | Purpose and envelope |
| --- | --- |
| `moonwatch_observatory` | Non-enterable dome inside the building's 3.8m collider and 9m height: every vertex within radius 3.60m (measured 3.55m) and y from -.80 to 8.85m (measured -.80 to 8.81m), the buried ashlar skirt absorbing the .45m the hill drops across the footprint. It has no cutaway contract. Two staggered courses of bevelled ashlar from -.80 to .55m at radius 3.10m under a .08m coping; 88 bevelled silvered staves (.20 wide, .10 deep) from .55 to 4.55m at radius 2.95m over a sealed `recess_shadow` backing cylinder, bound by three `pale_frame` hoops at 1.30 / 2.70 / 4.10m with eight verdigris clasps each; a `moon_ashlar` cornice walkway at 4.55–4.85m, radius 3.35m, with 20 verdigris rail posts and a rail ring at 5.60m; a hemispherical dome from 4.85 to 8.05m with eight verdigris ribs meeting a bronze collar, 56 single-quad `moon_glass` panels at .03m rib clearance, two purlin rings and a closed observation-slit hatch down the +Z rib pair; a verdigris finial to 8.55m under a brass star orb centred at 8.65m; a recessed closed `pale_frame` door 1.10 × 2.30m with three hinge straps, a brass knob and a star plaque on +Z, and a `moon_glass` porthole in a verdigris ring on each local ±x at 2.90m. Two authoring deviations from the card are worth keeping: each hoop stops either side of the door and both portholes and ends in a clasp rather than running through them, and the three steps are annular sectors hugging the plinth out to z 3.55m (widths 2.0 / 1.7 / 1.4m, rises .24 / .18 / .18m, treads about .20m) rather than straight boxes with .30m treads. |
| `star_telescope`, `star_table`, `chart_crate`, `moon_lantern` | Replacements for the four original observatory sites, footed at the recorded positions and radii (1.4, 2.0, 1.1 and .55m; measured 1.25, 1.62, .97 and .45m, tops 2.23, 1.27, 1.13 and 2.59m against the 2.8, 1.4, 1.3 and 2.7m envelopes): a brass tube with three verdigris bands, a verdigris eyepiece and a moon-glass objective on a pale tripod beside a step stool; two stacked ashlar drums with an inlaid verdigris ring, eight radial lines, a tilted brass gnomon, rolled and open charts and a brass compass rose; a pale crate with verdigris corner straps, its lid ajar over five scroll tubes with brass end caps, a folded chart and a leaning satchel; an ashlar footing and pale silvered post carrying a verdigris bracket and four-post cage around a `moon_glass` block under a brass crescent. |
| `armillary_sphere`, `scholar_bench` | Loose props placed by clearance (radius .8 and 1.1m; measured .58 and .92m, tops 2.06 and .94m): an ashlar pedestal with a verdigris base ring, three verdigris rings tilted 0 / .6 / 1.2 rad and a brass axis rod and orb; a pale 1.8 × .40m bench seat at .48m on two ashlar block feet with a low back rail to .92m, an open book and a rolled chart. |
| `silver_tree_a`, `silver_tree_b` | Curling silver trees, radius under 3.4 / 3.8m (measured 3.33 / 3.78m) and 9 / 11m tall (measured 8.57 / 10.65m): a sinuous tube trunk tapering .34 to .12m, three to four branches each curling through more than 120° around the trunk axis as they rise, two or three tapering twigs per branch, and 112 / 148 lavender leaf clusters spread along the outer half of every limb rather than at the tips, with six hanging `moon_glass` glow motes. Every `silver_bark` vertex below 1m stays within .40 / .44m of the axis (measured .345 / .392m) so a scaled trunk sits inside a recorded collider. |
| `moon_mushroom` | Luminous grove mushroom, radius under 1.80m (measured 1.76m) and 3.30m tall (measured 3.29m): a leaning pale stem, a broad lilac cap 1.65m across centred .15m off-axis, a `moon_glass` gill annulus at 2.55m under the cap and five pale spots. |
| `moon_crystal` | Three to five faceted `moon_glass` shards, the tallest 1.5m, on a small `moon_ashlar` rock base; radius under .60m (measured .44m), top 1.60m (measured 1.51m), two material primitives. |
| `moon_boulder` | Rounded pale moon-grey boulder with a shoulder lump and four embedded `moon_glass` shard clusters in its flank; radius under 2.20m (measured 1.99m), y from -.40 to 4.00m (measured -.40 to 3.60m). It dresses the collidable rock `prop-33` at `(109, 57)` (radius 2.2, height 4) at scale 1. |
| `moonbell_clump` | Seven arching thin stems each ending in a hanging five-sided bell, exactly one `moonbell` primitive, radius under .50m (measured .43m) and .70m tall (measured .49m). |

The exporter validates the observatory's radius and y range (3.60m, -.80 to 8.85m) against its 3.8m collider and 9m height, every prop envelope above, both trunk radii, `moonbell_clump` as exactly one primitive and `moon_crystal` at no more than two, zero images, every `COLOR_0` slot average at .70 or above, and a 3.5MiB / 40k-triangle guard. Tests recheck all of that from the shipping GLB, together with the work-site lookup by four distinct radii (1.4, 2.0, 1.1 and .55m), the dressed obstacles' runtime scales, the `moon_glass` emissive binding and the manifest's `buildingContracts`, `propEnvelopes` and `trunkRadii` fields.

## Materials and texture scale

Five small original texture families are shared: stone, timber, slate, earth and foliage. Stone, timber and earth use 512 × 512 base color and normal maps; slate and foliage use 256 × 256. They are tileable and embedded in the GLB. Base color is sRGB. Tangent-space normal maps are linear/non-color, with glTF's +Y convention; normal pixels use six-bit precision per channel to keep the asset compact. Roughness is a material scalar, not a painted shadow map. No directional light or ambient occlusion is baked into the textures.

Explicit metric UVs provide approximately meter-scale masonry/soil detail and longitudinal timber grain over roughly 2.2m of beam length. Vertex colors add per-stone variation, moss near damp ground and color variation in vegetation. Preserve `COLOR_0` when changing the exporter. Each geometric blade or needle spray has a modeled outline; there are no transparency cards or alpha sorting dependencies. Foliage is double-sided; masonry, timber and bark use outward-facing surfaces.

Farm reuses those texture families with explicit named slots. `watch_stone`, `aged_timber`, `forged_iron` and `recess_shadow` retain the source material; `farm_plaster`, `farm_roof`, `crop_straw`, `crop_leaf` and `hay_straw` clone the corresponding shared material with linear color multipliers recorded in the manifest. UVs and `COLOR_0` still provide scale and individual weathering. Multipliers above one deliberately brighten the darker shared surfaces for limewash and straw. Do not treat these linear multipliers as sRGB display colors.

Tideglass also clones named shared materials without copying their textures. Its manifest records the exact brighter linear multipliers for plaster, timber, cream/coral/teal canvas, teal roofs, foliage and produce. Plaster normal scale is .28 and canvas .18; canvas has modeled reverse faces so its underside survives binding to the shared front-sided earth material. Gameplay review, rather than the offline preview alone, determined the brighter coastal palette. Clones remain owned by their area and never dispose borrowed textures.

Saltwind clones the same families with its own named slots (manifest and `client/saltwind-harbor.js` are kept identical and checked by test): `tar_timber`, `salt_plank`, `oak_frame`, `tavern_wash`, `cottage_wash` and `sea_blue` on `aged_timber`; `shake_roof`, `harbor_tile` and `faded_tile` on `dark_slate`; `net_cord` on `needle_foliage`; `float_cream`/`float_coral` on `ground_earth`; `lantern_amber` unchanged. Two calibration facts are worth keeping: the shared base textures are dark (`aged_timber` averages about 104/93/74 sRGB, `dark_slate` 58/70/76), so a surface meant to read light in the third-person camera needs multipliers in the 5–10 range and a reduced normal scale (.28–.5), otherwise micro-shadowing keeps it near black however high the tint; and the bluish slate base fights warm roofs, so terracotta and salmon need red near 10 with green and blue at 2–4. Judge these in the running game beside an already-shipped kit; the Cycles preview lights them differently.

Driftwood Yard binds thirteen slots (manifest and `client/driftwood-yard.js` kept identical and checked by test): `raw_plank` (5.4/4.1/2.3, normal .45), `pitch_black` (.55/.5/.45), `tar_roof` (.85/.76/.66, lifted so the shed roof is not a black hole), `oxblood_paint` (3.6/1.05/.9) and `bleached_wood` (5.8/5.7/5.3, normal .30) on `aged_timber`; `silver_shingle` (5.8/5.4/4.3) on `dark_slate`; `sail_canvas` (8.5/8.0/6.8, `doubleSided`) on `ground_earth`; `hemp_rope` (2.6/1.7/1.1) on `needle_foliage`; and the five shared slots unchanged. Two in-engine corrections are worth keeping: the first oxblood tint (4.6/1.5/1.1) read orange and brick-like and was deepened, and silver shingles need blue pulled down to about 4.3 or the bluish slate base reads as Old Watch slate. A binding may declare `doubleSided`; the runtime then sets the clone's side, so single-sided pennant triangles read from both sides without modelled reverse faces.

Palmheart binds sixteen slots (manifest and `client/palmheart-camp.js` kept identical and checked by test): `jungle_hardwood` (1.6/1.15/.85, normal .5) and `charred_wood` (.45/.40/.36) on `aged_timber`; `jungle_bark` (1.35/1.2/1.05, normal .8) on `pine_bark`; `woven_mat` (4.6/3.9/2.4, normal .35) and the double-sided `canvas_flap` (7.5/7.0/5.6) on `ground_earth`; the double-sided `palm_thatch` (3.0/2.3/1.0, normal .6), `broad_leaf` (.85/1.35/.75), `palm_frond` (1.05/1.45/.70) and `elephant_ear` (.75/1.30/.68) plus `hemp_rope` (2.6/1.7/1.1) on `needle_foliage`; and the six shared slots unchanged. The foliage base is already green, so straw needs red well above green: the first thatch tint (2.4/2.1/1.0) with a green eave ramp rendered as a lime blanket in the game, and the shipped values put red at 3.0 with an olive-to-straw vertex ramp. Bark and hardwood share one timber texture, so the buttress fins' `jungle_hardwood` slot carries a vertex multiplier equal to the ratio of the two tints and reads as the same trunk.

The Cinderworks binds fourteen slots (manifest and `client/cinderworks.js` kept identical and checked by test): `basalt_block` (1.75/1.45/1.38, normal 1.0), `soot_stone` (1.0/.88/.85, normal .8), `ore_rock` (1.45/.95/.62), `scoria` (1.05/.70/.62) and `coal` (.30/.29/.29) on `watch_stone`; `charred_board` (1.15/.90/.72, normal .45) on `aged_timber`; `rust_sheet` (2.3/1.35/.95, normal .40) on `ground_earth`; `sign_plate` (1/1/1) on `forged_iron`; `ember_crystal` (1.15/.75/.50) on the emissive `lantern_amber`; and the five shared slots unchanged. The calibration fact worth keeping: the basalt courses carry vertex colours averaging .45 for their soot gradient, so the first tint (.70/.60/.58) multiplied to about .3 of an already dark stone and rendered the walls, boulders and outcrop as black silhouettes; the shipped basalt tint sits near 1.75 with green and blue pulled down to neutralise the shared stone's moss, and soot stone near 1.0 still reads black. Rust on the mottled earth texture reads as corroded sheet without a new image; its first value (2.4/1.25/.80) was slightly too orange and gained blue.

Moonwatch binds twelve slots (manifest and `client/moonwatch.js` kept identical and checked by test): `moon_ashlar` (2.30/2.25/2.45, normal .70), `mushroom_cap` (3.40/2.60/8.00, normal .35) and the double-sided `moonbell` (5.60/4.60/8.00, normal .40) on `watch_stone`; `silver_stave` (4.40/5.60/9.20, normal .35), `pale_frame` (5.60/5.50/5.40, normal .30) and `mushroom_stem` (4.40/4.50/4.30, normal .30) on `aged_timber`; `silver_bark` (3.90/4.90/8.60, normal .70) on `pine_bark`; the double-sided `lavender_leaf` (6.50/2.40/20.0, normal .50) on `needle_foliage`; `verdigris_bronze` (1.90/3.40/3.00) and `bright_brass` (7.50/5.60/2.60) on `forged_iron`; the double-sided `moon_glass` (.80/2.90/12.0) on `lantern_amber`; and `recess_shadow` unchanged. The shared ferns and grass clumps are tinted (3.0/1.4/7.0) and (3.2/1.6/7.5) at runtime. This kit introduces two binding keys: `emissive: [r, g, b]` in linear RGB, which the runtime applies with `material.emissive.setRGB`, and `emissiveIntensity`. Only `moon_glass` uses them (emissive .16/.40/.44 at intensity 1), and it is the kit's only light: the grove glows from an emissive material, not from a scene light. The calibration facts are worth keeping because a cool kit inverts the earlier ones. Measured from the shared GLB's PNGs, `aged_timber` and `pine_bark` both average 104/93/74 sRGB, `ground_earth` 86/77/57, `needle_foliage` 83/108/54 and `watch_stone` 96/98/89 — linearly about .137/.108/.068 for timber, .094/.075/.041 for earth, .087/.152/.037 for foliage and .117/.122/.100 for stone. Rendered albedo is approximately texture × tint × the slot's `COLOR_0` average, so a tint is target ÷ (texture × `COLOR_0`), and silver or lilac over a warm timber or green foliage base needs blue in the 8–20 range with green well below red. The first in-engine pass ignored that and failed on five slots at once: leaves at 1.55/.95/1.85 read as dead olive, bark at 2.6/2.5/2.9 as ordinary brown, staves at 4.8/5.0/5.6 as warm tan indistinguishable from the ashlar beneath them, caps at 2.2/1.6/3.2 on `ground_earth` as dark plum, and moonbells at 2.4/2.1/2.9 on `needle_foliage` as green. The shipped values above fixed all five without touching the GLB, and two of them also moved `source`: `mushroom_cap` from `ground_earth` and `moonbell` from `needle_foliage` to the neutral `watch_stone`, because no multiplier lifts a saturated base off its own hue. The moss overlay shifted with them, from grey (`#6d6a86` / `#a9a6b8` / `#c4bfcf`) to lilac (`#7a72a2` / `#b3adcc` / `#cfc9de`).

### Area identity contract

One construction and weathering language, never one colour scheme. Every area writes a palette card before modelling and compares it with the shipped rows below; a new card may not repeat another row's wall treatment + roof colour pair, and buildings within one area should differ from each other in at least siding, trim or roof. Materials may share source textures — the tint, normal scale, siding construction and accent props are what carry identity.

| Area | Wall treatment | Trim / frame | Roof | Accents | Ground | Light |
| --- | --- | --- | --- | --- | --- | --- |
| The Old Watch | Mossy grey-green rubble masonry, silvered timber infill | Forged iron, dark recesses | Dark overlapping slate | Warm lantern glass | Earth, tawny grass, ferns, boulders | Cool and misty |
| Windward Farm | Aged limewash over fieldstone; warm barn boards | Warm barn timber | Russet clay tile and shingled mill cap | Framed sails, golden wheat, leafy crops, hay | Worn field edges, split-rail fences | Warmer, slightly hazy |
| Tideglass Market | Pale weathered plaster over stone | Pale aged timber | Teal tile | Cream/coral/teal canvas, produce, cloth bolts | Sandy routes, pavers, broadleaf shrubs | Bright coastal |
| Saltwind Harbor | Net-house: unpainted salt-grey board-and-batten. Tavern: cream lime-washed clapboard. Cottage: whitewashed shiplap | Tar-black battens; honey oak; sea-blue shutters | Green cedar shakes; terracotta shingle; sun-faded salmon tile | Nets and cork floats, lobster pots, crates, amber lantern, fish sign | Pale sand, shell-grit paths, straw dune grass, tarred pilings | Airy and warm |
| Driftwood Yard | Shed: raw honey waney-edge weatherboard. Cottage: oxblood-painted clinker | Pitch-black tarred posts and sills; bleached driftwood-white boards and shutters | Tarred boards with bleached battens; silver-grey wooden shingle | Ribbed hull on cradles, timber stacks, sawhorses and bow-saw, pitch kettle, half-hull sign, gull finial | Sawdust and worked sand, dark trodden paths, sparse sea oats | Warm and golden |
| Sunwake Strand | None (landing only) | Bleached pier decking on tarred pilings | — | Sailcloth pennant line, rope-tied landing crates, fingerboard signpost, driftwood logs | Near-white open sand, damp tideline band, pale routes, sea-oat grass | Brightest and airiest |

| Palmheart Camp | Woven palm matting on a lashed dark hardwood pole frame (the tent) | Dark jungle hardwood poles, hemp lashings; iron only on the lantern and tripod | Layered palm-frond thatch, green-gold to straw | Sooted fire ring, charred logs, tripod kettle, split-log benches, drying rack and hung gear, water cask, coral trail-keeper pennant, thatched lantern hood | Dark humus and leaf litter, trodden red-brown path, packed clearing | Humid and green-filtered |
| Palmheart Wilds | None (vegetation only) | — | — | Buttressed broadleaf hardwoods, ringed jungle palms, elephant-ear clumps, ferns | Jungle floor fading into the original green | Same humid profile |
| The Cinderworks | Coursed black basalt block with a soot backing; oiled charred vertical boards above | Riveted iron straps, iron-banded jamb posts and corner posts, iron bracket and hammer sign | Lapped rust-red iron sheet with rivet rows and a basalt-and-soot chimney with iron cowl | Anvil on a basalt block, quench trough, hammer and tongs, ore pile with ingots and strapped crate, coal bin and shovel, slag heap with ember glints, iron lantern post | Black ash and cinder, soot rings, scorched orange-brown tracks, flagstone floor | Warm ash haze: closer dusty fog, amber sky light, ember-brown bounce |
| Emberpeak apron | None (scenery only) | — | — | Columnar basalt outcrop, basalt boulders, scoria cinder clumps, amber ember crystals, ochre scorched grass | Ash feathering into the original volcano tan | Same ash-haze profile |
| Moonwatch Observatory | Round drum of pale silvered vertical staves bound by three pale wood hoops, over a plinth of pale moon-grey cut ashlar with tight joints and no moss, up three ashlar steps to the door | Verdigris bronze dome ribs, rail posts, hoop clasps, door hinges and telescope mount; pale wood door and porthole frames; polished brass on the instruments | Ribbed dome of pale luminous moon-glass panels between verdigris bronze ribs, a closed bronze observation-slit hatch, a bronze finial with a brass star orb | Brass telescope on a pale tripod with a step stool, round ashlar star table with an inlaid bronze ring, gnomon and charts, chart crate with rolled scrolls, armillary sphere on a pedestal, scholar's bench, cool cyan moon-glass lantern | Silver-lilac moss and pale mineral gravel, pale trodden path to the door and along the trail | Cool luminous: lilac-tinted sky, closer lilac fog, pale cyan-lilac sky light, lilac-grey ground bounce, slightly cooler sun |
| Moonbloom Grove | None (scenery only) | — | — | Curling silver trees, glowing moon mushrooms, moon crystals, moonbell clumps, pale ferns and grass | Same overlay feathering into the original lilac ground | Same cool luminous profile |

Every planned building row is now shipped and milestone 9 is island-wide finishing rather than a new area, so any later building has to start again from what is *not* on this table.

## Shared ownership and total cost

`client/environment-assets.js` owns local GLB loads and decoded resources. Area controllers acquire leases, share the in-flight promise and source geometry/material/texture/ImageBitmap ownership, then release idempotently. Area-created material clones retain borrowed textures; releasing a clone must not dispose the shared maps. Staged instances are installed atomically, and failed or late completion cleans up area resources before releasing source leases. Legacy building and scenery batches remain visible until the full area is ready; failed loading leaves a playable fallback.

Old Watch's existing GLB is the first shared library. It is loaded once per live cache even when both controllers need it. Farm depends on that entire library, including the original building geometry; this milestone does not split it into independent prop/texture downloads or ship distance-triggered asset streaming. A later measured need can justify splitting the library while maintaining the ownership contract. Copying the complete Old Watch kit into another area is not the extension pattern.

| Shipping kit | GLB bytes | Source triangles | Material primitives | Embedded images | Added decoded RGBA8 + full mip chain estimate |
| --- | ---: | ---: | ---: | ---: | ---: |
| Shared Old Watch | 7,584,620 | 59,078 | 33 | 10 | 9,786,696 bytes |
| Windward Farm additions | 2,459,180 | 26,740 | 30 | 0 | 0 bytes |
| Tideglass Market additions | 2,019,240 | 22,258 | 45 | 0 | 0 bytes |
| Saltwind Harbor additions | 3,263,132 | 35,510 | 103 | 0 | 0 bytes |
| Driftwood Yard and Sunwake Strand additions | 2,432,348 | 26,569 | 82 | 0 | 0 bytes |
| Palmheart Camp and Wilds additions | 2,792,952 | 28,831 | 38 | 0 | 0 bytes |
| Cinderworks and Emberpeak additions | 2,768,008 | 30,284 | 56 | 0 | 0 bytes |
| Moonwatch Observatory and Moonbloom Grove additions | 2,444,180 | 26,784 | 43 | 0 | 0 bytes |
| Unique combined total | 25,763,660 | 256,054 | 430 | 10 | 9,786,696 bytes |

These are download/source costs, not triangles or draw calls in a gameplay frame. Source parts are reused and instanced, and shadows can draw a visible mesh again. The texture estimate excludes browser decode copies, driver allocation overhead, render targets and other game textures. `world.getStats()` exposes renderer triangles/calls alongside `environmentAssets`, `oldWatch`, `windwardFarm`, `tideglassMarket`, `saltwindHarbor`, `driftwoodYard`, `palmheartCamp`, `cinderworks` and `moonwatch` loading/installation statistics; measure warm views and populated gameplay as the rollout grows. Saltwind is the largest single kit because it authors three buildings at once; its guard is 3.5MiB and 42k source triangles, and like the others it is not an allocation for later areas.

Old Watch's 8MiB/120k-triangle ceiling remains a per-kit failure guard. Farm's additions stay below 2.5MiB and 32k source triangles. Neither limit is an island-wide budget allocation. The present tradeoff retains uncompressed meshes and ten original PNG images. Small Farm foliage/field detail uses spatially bounded instance batches and distance/quality reductions; architecture remains visible at distance for ship and glider approaches. Avoid multiplying material draw calls by creating a separate object for every stone, tile or frond.

Tideglass's guard is 2.3MiB and 24k source triangles. Its 190 grass clumps, 32 shrubs and 69 low-poly pavers use 46 bounded detail meshes with 16m cells. High/low detail distance is 105/65m; low quality uses 42% of plant instances, and distant high-quality planting uses 65%. Architecture remains visible for aerial approaches. The three loaded kits share five leases and the same ten texture images. These per-area guards still do not authorize multiplying the same cost across the island.

Driftwood's guard is 3.5MiB and 40k source triangles; the first build came in at 4.9MB / 52.8k and was cut to 2.4MB / 26.6k with unbevelled siding courses, fewer knots, ten shingle columns, three-to-seven-sided ropes and seven hull stations before anything shipped. Its 139 sea-oat clumps use fourteen 16m cells with the market's 105/65m distances and 42% low-quality density; the pier's five sections, two poles, one banner line, two crate stacks, six logs and the signpost are plain clones, so the strand costs about 50 draw calls over the baseline landing view. The five coastal kits share nine leases and the same ten texture images.

Palmheart's guard is 3.0MiB and 36k source triangles. Its first export (19.8k triangles) read as bare saplings in the game and was deliberately grown, not slimmed: the two hardwoods went from 51 and 60 leaf clusters at the branch tips to 78 and 102 clusters along the outer half of every limb, 8.0k and 10.0k triangles each, and the kit ships at 28.8k. The 28 seeded trees plus the dressed collidable tree are instanced per prefab and material in 64m cells (three source primitives per hardwood, two per palm), cast shadows and are never distance-hidden because the canopy is the area's silhouette from the ship; 90 elephant-ear clumps, 140 ferns and 110 grass clumps use 26 16m cells with the market's 105/65m distances and 42% low-quality density. The camp props are plain clones with 7–8 material primitives each, so the tent and three sites cost about 30 draw calls before shadows. The kit adds no scene light: a fire point light was tried and removed, because a second point light changes the light count of every island shader for a glow that daylight hides anyway. The six loaded kits share eleven leases and the same ten texture images.

The Cinderworks' guard is 3.5MiB and 40k source triangles; it ships at 2.64MiB and 30.3k, most of it the forge (23.4k across six parts, because every basalt course is a bevelled block and every strap carries rivets). Its 16 boulders are instanced per prefab and material in 32m cells that cast shadows and never distance-hide, like Palmheart's canopy; 120 cinder clumps, 22 ember crystals and 90 scorched grass clumps use 27 16m cells with the market's 105/65m distances and 42% low-quality density. The building, sign, three sites, two loose props and the outcrop are plain clones, so the forge square costs about 70 draw calls over the baseline view. The ember glow comes from the shared emissive amber material, not a light. The seven loaded kits share thirteen leases and the same ten texture images.

Moonwatch's guard is 3.5MiB and 40k source triangles; it ships at 2.33MiB and 26.8k, 15.3k of it the observatory (88 bevelled staves, 56 glass panels and 8 dome ribs are most of that). Its 22 silver trees are instanced per prefab and material in 64m cells and its 24 mushrooms in 32m cells; both cast shadows and never distance-hide, because the curling canopy and the glowing caps are the grove's silhouette from the trail and the air. The 26 crystals, 110 moonbell clumps, 90 ferns and 90 grass clumps use 16m cells with the market's 105/65m distances and 42% low-quality density. The building, four work sites, two loose props, the dressed boulder and the dressed collidable tree are plain clones, so the kit costs about 65–85 draw calls and 230–260k renderer triangles over the baseline dome, trail and grove-edge fixtures. The glow comes from the emissive `moon_glass` binding; no scene light is added. The eight loaded kits share fifteen leases and the same ten texture images.

## Placement, collision and lighting

`shared/old-watch.js` owns stable authored pine and rock placements and their collision data. `shared/world.js` appends those colliders without changing existing obstacle indices. Gameplay collision is sourced from these shared shapes, not inferred from GLB visuals. Keep trunk/rock silhouettes inside the corresponding collision envelopes after runtime scaling, and keep resident routes, loot, both doors and the route to the farm clear.

`shared/windward-farm.js` derives the connector from the existing exploration trails and owns deterministic field, fence and planting clearance. Building IDs, obstacle IDs, resident loops, chests and doors remain authoritative in shared gameplay data. Farm adds no blocking obstacles: its small surface stones are walkable and fences retain the existing cosmetic semantics. Existing procedural scenery consumes the original RNG stream before routing the two areas' fallback batches, preserving unrelated regional placement.

`client/environment-geometry.js` owns the actual rendered-height helper. Ground overlays follow the alternating triangles of the existing 2m terrain grid, with smooth vertex-alpha boundaries and no new gameplay elevation. Do not sample only the analytical height function or assume every cell uses the same diagonal. Small plants use seeded instanced placement; low quality reduces decorative density/draw distance, and reduced motion freezes sway and the original mill animation convention. Barn furnishings and its existing paired-door/cutaway behavior are retained when authored faces and roof install.

`client/environment-lighting.js` is the single owner of global sky, fog, hemisphere and sunlight. It captures an immutable baseline, blends normalized overlapping area weights and restores the baseline outside the areas and aboard ship. Old Watch's center profile stays the approved profile. A ready Farm adds a restrained warmer profile along the field and connector; Old Watch's former Farm lighting exclusion is removed only when Farm is ready. Farm failure keeps the original exclusion and baseline transition. Area animation controllers do not compete to write global lighting each frame.

`shared/tideglass-market.js` bounds the market and immediate Haven dressing and excludes primary/exploration paths, resident loops, both cottage approaches, loot, work props and the objective dais from planting. Its ground follows the same alternating terrain triangles with transparent fades and a cottage/dais exclusion. Existing palm canopy geometry is retained. Only the original hut geometry and small ground vegetation are separated into complete fallbacks; the full original scenery triangle multiset and next RNG value remain regression-tested. Tideglass adds a ready-dependent coastal profile to the one lighting owner; market-only failure leaves Watch/Farm authored, while shared-library failure restores all three procedural areas and baseline lighting.

`shared/saltwind-harbor.js` bounds the harbor to its square plus a halo around each of the three buildings, keeps the Sunwake landing (x > -4) and the water at baseline, and finds the four original work sites by their authored radii the way the farm finds its field. `client/settlement.js` marks all three harbor buildings authored, routes the generic work-site props into the harbor fallback group, and swaps every harbor interior's cutaway walls and roof at once; the fallback (buildings, base slabs and work sites) hides only after the whole kit installs. The sand overlay follows the alternating 2m terrain triangles at +.034m, fades at its edges and skips all three floors; dune grass reuses the shared `grass_clump` in 16m instanced cells with the market's 105/65m distances and 42% low-quality density; nets sway through one wind uniform that reduced motion zeroes. The tavern chimney is authored at the fallback's smoke emitter so the retained plume needs no new animation owner. Lighting adds an airier, warmer harbor profile to the one lighting owner.

`shared/driftwood-yard.js` bounds two areas from one kit: the yard is its radius plus a halo around both buildings, gated to x > 8 and z > 70; the strand is a 25m disc around the landing, crossfading with the harbor over x in [-9, -4] and with the yard over x in [8, 17]. It finds the three original work sites by radius, exports the landing crate candidates and the same point-of-interest filter that `buildScenery` applies to them, and its plant clearance additionally keeps 9m around the drop point, 5m around the yard's defense supplies and 8m around the guarded inland approach. `client/settlement.js` marks both yard buildings authored and routes the hull, timber stack and lantern sites into the yard fallback group; `client/world.js` routes the original dock planks, flag poles, pennant cones and crates into a `sunwake-strand-original-landing` mesh and the small ground plants into a yard legacy mesh, both hidden only after the whole kit installs. The pier places five `pier_section` clones at `max(1.15, renderedHeightAt + .14)` and pitches each about X toward its neighbours, so the deck follows the beach into the water like the original planks did. Both overlays follow the alternating 2m terrain triangles at +.034m with no depth bias; the yard's skips both floors and the strand's fades out below .25m height. Pennants and sea oats share one wind uniform that reduced motion zeroes. Lighting adds a golden yard profile and the island's brightest strand profile to the one lighting owner, gated on the kit being ready.

`shared/palmheart-camp.js` bounds the camp to its radius plus a 9m halo, unions a corridor along the shrine trail, and cuts both back with an 11m exclusion around the shrine so the dais, its 9m active ring and the primary route junction keep the baseline. It adds nothing to `OBSTACLES`: authored trees are walkable decorations like the original jungle canopy, and `PALMHEART_CANOPY_OBSTACLES` lists the existing collidable jungle trees inside the weight (today `prop-9` at `(-98, 18)`) so the runtime can dress them with a scaled hardwood whose trunk stays inside the recorded radius; the seven camp guards spawn from those same obstacles around `(-88, 12)`. Plant clearance keeps 2.8m off every trail, 6m off the primary shrine route, 12m off the shrine, 8m off the encounter group, 2.4m off chests, 1.25m off Bram's loop and the recorded radii off the work sites. `client/settlement.js` routes the tent and the three camp sites into a `palmheart-camp-original-exterior` group, together with the coral flag pivot, and hides it only after the whole kit installs; `client/world.js` routes the collidable tree, the decorative trees and the small ground plants inside the weight into a `palmheart-wilds-original-vegetation` mesh. The jungle floor follows the alternating 2m terrain triangles at +.034m with no depth bias and fades below 1.7m. Trees are instanced in 64m cells that cast shadows and never hide with distance; elephant ears, ferns and grass use 16m cells with the market's 105/65m distances and 42% low-quality density. Leaves, fronds, thatch and the pennant share one wind uniform that reduced motion zeroes; trunks, poles and stone stay rigid. Lighting adds a humid green profile (closer fog, green sky light, deep green ground bounce) to the one lighting owner, gated on the kit being ready.

`shared/cinderworks.js` bounds the forge yard to the point of interest's radius plus a 9m halo, unions a 12m halo around the forge itself, and cuts both back with a 14m exclusion around the ember shrine so the dais, its ring, the caldera crescent and the primary route junction keep the baseline; all three ramps are 9m wide so the ambient never steps more than .01 per .1m anywhere on the island. It adds nothing to `OBSTACLES`: `CINDERWORKS_ROCK_OBSTACLES` lists the collidable rocks inside the weight (today `prop-23` at `(69, -34)`) so the runtime can dress them with the outcrop scaled to the recorded radius and height. Plant clearance keeps 2.8m off every trail, 2.1m off Sula's loop and both door approaches, 6m off the primary shrine route, 12m off the shrine, 8m off the encounter group at `(80, -55)`, 2.4m off chests, 1.25m off resident routes and the recorded radii off the work sites; the three sites are found by their distinct radii. `client/settlement.js` marks the forge authored, routes its anvil, ore and lantern sites into a `cinderworks-original-exterior` group and swaps the forge's cutaway walls and roof at once; `client/world.js` routes the collidable rock's pebbles, the decorative crystals and pebbles and the small ground pebbles inside the weight into a `cinderworks-original-scenery` mesh, both hidden only after the whole kit installs. The ash overlay follows the alternating 2m terrain triangles at +.034m with no depth bias, skips the forge floor and fades with the square of the weight so the apron feathers into the original tan rather than ending in a disc. The hanging sign and the scorched grass share one wind uniform that reduced motion zeroes; basalt, iron and heaped scoria stay rigid. Lighting adds a warm ash-haze profile (closer dusty fog, amber sky light, ember-brown ground bounce, slightly reddened sun) to the one lighting owner, gated on the kit being ready.

`shared/moonwatch.js` bounds the observatory grounds to the point of interest's radius plus a 9m halo, unions a 12m halo around the dome itself, and cuts both back with a 14m exclusion around the Moonbloom Shrine so the dais, its 13.7m crystal ring, the stone gate and the primary route junction keep the baseline; all three ramps are 9m wide, and a .1m scan of `x ∈ [68, 126], z ∈ [14, 72]` with all eight kits ready measures a worst ambient step of .0083, inside the .01 per .1m rule. It adds nothing to `OBSTACLES`: `MOONWATCH_CANOPY_OBSTACLES` lists the collidable trees inside the weight (today `prop-28` at `(109, 35)`) so the runtime can dress them with a scaled `silver_tree_b` whose trunk stays inside the recorded radius, and `MOONWATCH_ROCK_OBSTACLES` the collidable rocks (today `prop-33` at `(109, 57)`) so the runtime can dress them with `moon_boulder`. Plant clearance keeps 2.8m off every trail, 2.1m off Lio's loop and the door approach, 6m off the primary shrine route, 12m off the shrine, 8m off the encounter group at `(97, 31)`, 2.4m off chests, 1.25m off every resident route and the recorded radii off the work sites; the dome is non-enterable, so it reserves its radius plus .6m rather than a doorway box. The four sites are found by their distinct radii (1.4, 2.0, 1.1 and .55m). `client/settlement.js` routes the dome fallback together with the three observatory site props and the lantern marker into a `moonwatch-original-exterior` group and hides it with `setMoonwatchKit` only after the whole kit installs; `client/world.js` routes the moon-region decorative trees, mushrooms and ground plants inside the weight and both collidables into a `moonwatch-original-scenery` mesh, leaving the shrine's four large mushrooms, its crystal ring and the gate ring in the original batch. The moss and gravel overlay follows the alternating 2m terrain triangles at +.034m with no depth bias, skips the dome footprint and fades with the square of the weight so the grove feathers into the original lilac ground rather than ending in a disc. Leaves, moonbells, ferns and grass share one wind uniform that reduced motion zeroes; bark, glass, bronze, ashlar and the mushroom caps stay rigid. Lighting adds a cool luminous profile (lilac sky at .70 strength, closer lilac fog 82/365, pale cyan-lilac sky light, lilac-grey ground bounce, ambient 1.70, a slightly cooler sun at 2.30) to the one lighting owner, gated on the kit being ready. The telescope and chart-crate sites sit at weight .34 and .37 because both fall inside the shrine ramp about 17.5m out; that was accepted rather than widening the area, because pushing the weight to 1 there would move the shrine, its ring and the primary route off the island baseline for two props, and at a third of the weight the moss reads as a faint dusting and the light as two thirds of the profile.

Judge changes in the actual third-person gameplay camera as well as the offline preview. Check the tower front, courtyard, both doors, interior cutaway, gliding/roof view and transition to the farm at high and low quality. Inspect the console and network tab for asset/texture failures; deliberately block the GLB once to confirm fallback. Run the relevant automated tests, `npm test` and `git diff --check` before committing and deploying.

## Windward Farm milestone QA — 2026-09-05

Measured on this host in Chrome/WebGL2 at 1920 × 1080, device pixel ratio 1. Matching five-player fixed-render fixtures sampled 240 frames after 60 warm-up frames. Their poses were static and contained no enemies; browser scheduling, vsync and platform limits affect these timings. Baseline `2c2d3fe` averaged 16.67ms with 16.8ms p95 in all four fixtures. The final-source comparison is:

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Final mean / p95 frame time |
| --- | ---: | ---: | ---: |
| Old Watch / high | 149 → 150 | 670,302 → 667,618 | 17.52 / 18.2ms |
| Old Watch / low | 148 → 149 | 500,254 → 497,570 | 17.51 / 18.2ms |
| Farm / high | 173 → 222 | 709,991 → 942,731 | 17.54 / 18.2ms |
| Farm / low | 172 → 221 | 539,943 → 723,427 | 17.51 / 18.2ms |

The current fixtures run at approximately 57 FPS on this host. Farm draw calls and triangles rose materially even while frame times remained close: this result is not a hardware guarantee or permission to multiply the same cost across later regions. Retain bounded batches and density/distance reductions, and benchmark each extension against the total island cost.

An isolated live five-client session exercised ordinary browser movement and interactions: opened chest 21 and the barn chest, traversed both barn doors in both directions and the connecting trail both ways, checked roof cutaway/restoration, turning sails, low graphics, reduced-motion freeze and the map. The populated world held 36 enemies, with seven within 28m of the farm, though not all seven were necessarily on screen. A warm populated sample measured 56.63 FPS and 18.2ms p95; one renderer snapshot reported 346 calls and 1,019,495 triangles. Combat and effects vary, so that snapshot is not a fixed scene budget.

Walking, barn-interior and aerial/gliding screenshots passed visual review. This review found and corrected floating hay placement and a sky slit below the mill cap's shingle fringe. No unexpected console or network errors remained. Deliberately blocking only Farm's GLB preserved authored Old Watch and the complete Farm fallback; blocking the shared GLB preserved both fallbacks and baseline sky at the farm. The loaded cache reported two URLs, three leases, ten shared images and 9,786,696 estimated decoded texture bytes, for 10,043,800 unique GLB download bytes. Both generators independently reproduced their shipping GLB and manifest byte for byte. Local screenshot/fixture helpers and output remain ignored QA aids and are not shipped.

Full `npm.cmd test` passed all 130 tests, including multiplayer progression and the environment regressions. `git diff --check` passed.

## Tideglass milestone baseline and repeatable review setup — 2026-09-05

Milestone 3 starts from `be58b78` (the preceding environment implementation remains `8b66888`). The existing market has the `fruit-stall`, `sailcloth-stall`, and furnished `market-cottage`; central Haven has the existing closed huts `prop-16` and `prop-17`. Their shared footprints, heights, paired cottage doors, resident loops and chest IDs are the authoring contract. The lighthouse and later coastal settlements are outside this milestone's architecture scope.

Baseline checks: `npm.cmd test` passed 136 tests; Docker `/health` returned `ok: true`, and 13 browser environment assets matched `git show HEAD:<path>` by SHA-256. Old Watch and Farm regenerated independently with the build commands above and their GLB/manifest bytes matched shipping source. Local captures had no console/page or failed-network errors.

Repeatable browser fixture: Chrome/WebGL2, 1920 × 1080 viewport, device scale factor 1. Serve the repository with an isolated `DATA_DIR` and unused `PORT`, or use the committed Docker service for read-only fixed-scene rendering. Build a local HTML fixture using the game's import map, import `createWorld` from `/world.js` and `heightAt` from `/shared/world.js`, and create a world on a full-viewport canvas. Use a `Game` snapshot with five players, phase `voyage`, elapsed 45 and no enemies. Wait until all environment controllers report ready. For each view, set the local player to ground mode and `y = heightAt(x,z)`, use the stated yaw/pitch, and settle `world.update(1/60, state, player, view)` for 120 iterations at time 12. For art stills, park the four peers at `(2i,94)`; for performance, increment `state.round` and place them at `(x - 3 + 1.5i, z - 3)`, with ground elevation and ground mode. Measure 300 `requestAnimationFrame` updates/renders per view/quality, discard the first 60, and report arithmetic mean and sorted 95th percentile alongside `world.getStats()`. This is a fixed-pose, five-player comparison, not combat or a hardware guarantee.

| Walking / close view | Player X, Z | Yaw, pitch (radians) |
| --- | --- | --- |
| Market square / performance | -27, 40 | 1.2, .02 |
| Fruit stall | -32, 31 | 1.45, .10 |
| Sailcloth stall | -24, 40 | 3.0, .06 |
| Cottage exterior | -35, 41 | 1.73, .03 |
| Cottage interior | -41, 41 | 1.73, -.13 |
| Haven west hut / performance | -14, 19 | .85, .03 |
| Haven east hut | 12, 31 | -.95, .03 |
| Beacon connection | 0, 17 | 0, .04 |
| Old Watch comparison / performance | -64, -76 | .60, .03 |

The aerial view uses player `(-29,39)`, then explicitly sets camera `(-7,30,61)` looking at `(-27,3,31)` after settling. The glider approach uses player `(-15,21,53)` in gliding mode with yaw .65 and pitch -.35. Keep these settings for the before/after comparison. Baseline performance was approximately 16.67ms mean / 16.8–17ms p95 across all six high/low fixtures; renderer costs are retained in the final comparison table below. Screenshots, generated fixtures, temporary save directories and reports belong in ignored QA storage; the instructions here are sufficient to recreate them without previous local helpers.

### Tideglass implementation QA — 2026-09-05

The final geometry adds **2,019,240 bytes**, **22,258 source triangles** and **45 material primitives**, with zero texture images. Its SHA-256 is `d43b7c1585bab1b2b6e69db5493189b6a1ba08702fe1409027f5c6bad4510690`. Independent background Blender 5.2.1 LTS regeneration reproduced both GLB and manifest exactly; the manifest SHA-256 is `0a6b32fbec40e901752850f895d55d08ecbdc0792568f4cf88d57866c62bf1a8`. Old Watch and Farm also reproduced their unchanged shipping bytes. The loaded cache reports three URLs, five leases, ten shared texture images and **12,063,040 unique GLB bytes / 9,786,696 estimated decoded texture bytes**.

Using the fixed comparison setup above, with the final sample served from Docker at implementation commit `2e2bc1d`:

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Final mean / p95 frame time |
| --- | ---: | ---: | ---: |
| Old Watch / high | 150 → 150 | 667,618 → 660,922 | 17.66 / 18.2ms |
| Old Watch / low | 149 → 149 | 497,570 → 490,874 | 17.73 / 18.4ms |
| Market / high | 178 → 215 | 361,373 → 393,993 | 17.81 / 18.5ms |
| Market / low | 177 → 214 | 361,213 → 376,689 | 17.74 / 18.3ms |
| Haven / high | 306 → 326 | 997,949 → 1,034,165 | 17.65 / 18.3ms |
| Haven / low | 274 → 294 | 636,055 → 652,071 | 17.76 / 18.6ms |

An earlier first-pass sample ran around 17.5–17.6ms with 18.1–18.3ms p95; the final preview before the paving depth-bias correction measured 16.67ms / 16.8ms p95 with identical renderer counts. The deployed repeat above was slower across all views, including Old Watch, illustrating host scheduling variability. These are renderer-frame totals, including other regions and shadows; the Watch triangle change is from other scenery visible to that view, not a changed Watch kit. Samples ran around 56–60 FPS under vsync and are not permission to multiply the new area cost across the island.

An isolated live server used five joined clients: one ordinary browser player and four WebSocket peers. With camp enemies temporarily withheld for navigation checks, browser W movement and E interactions opened chest 20 and the cottage chest, traversed both cottage doors in both directions, checked roof/wall cutaway and restoration, walked the market trail both ways and continued to the beacon. Map, low graphics and reduced motion worked; the new foliage wind uniform became zero, and the existing Farm sails froze. Restore the original enemy collection for the populated sample. The final live scene held **36 enemies**, with **five within 28m of the market** (not necessarily all on screen). Its warm sample averaged **16.67ms / 18.4ms p95**; one renderer snapshot was **301 calls / 455,763 triangles** with five players. Combat/effect timing and stationary peers limit comparability to other sessions.

Walking, close, cottage interior, both Haven huts, aerial/glider and Old Watch comparison views were inspected. Review brightened overly dark plaster/canvas, lowered beams protruding through sagged awnings and backed the cottage's low masonry. A close paving inspection found the overlay's negative polygon offset drawing soil over slabs despite correct winding: the actual slab tops sit only about 4mm above the already-raised overlay. Tideglass now uses its explicit 36mm terrain offset without polygon depth bias; close, ordinary, low and aerial views verify visible paving. No extra gameplay elevation was introduced.

To reproduce failure checks, intercept `/assets/tideglass-market/kit.glb` and then `/assets/old-watch/kit.glb` in separate fresh browser contexts and abort each request deliberately. The first preserves authored Watch/Farm plus complete market, cottage and hut fallbacks; the second preserves all three procedural areas. At market `(-27,40)`, both failed cases restore baseline sky `85d9ee`. No unexpected page/console/network errors remained in the successful runs, and failed contexts produced only the deliberately aborted request. Automated regressions additionally cover partial installation, independently arriving resources after failure/disposal, source nonmutation, exact-once shared disposal, roof undersides, full door prisms and original scenery/RNG preservation.

Final `npm.cmd test` passed all **152 tests**, including the final paving depth-bias correction. `git diff --check` and independent implementation review passed.

Implementation `2e2bc1d` was pushed to canonical `main` and deployed using `docker compose up -d --build --wait`. Compose reported healthy, `/health` returned `ok: true`, and 16 served environment assets matched `git show HEAD:<path>` by SHA-256, including all changed browser modules and the new GLB. The persistent volume mount was preserved. Deployed walking, interior and aerial screenshots and repeated independent/shared asset-failure checks passed without unexpected page/console/network errors. This evidence and the next bounded handoff are recorded in the rollout tracker.

## Saltwind Harbor milestone QA — 2026-09-06

Baseline `bf8fe6a` was served from the local Docker deployment; the final source ran from the working tree on an isolated Node server (`PORT=3491`, separate `DATA_DIR`). Both were driven through the ordinary client in Chrome/WebGL2 at 1600 × 900, device pixel ratio 1, one live player and no enemies withheld. The fixed comparison view is the harbor square facing the tavern front: player `(-22.9, 96.5)`, yaw `-.36`, pitch about `-.2`; `window.SKY.fps()` reports its rolling 240-frame window and `world.getStats()` the renderer counts.

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Baseline → final mean fps / p95 |
| --- | ---: | ---: | ---: |
| Tavern front / high | 646 → 670 | 785,520 → 807,292 | 56.8 / 18.3ms → 56.9 / 18.4ms |
| Cottage front toward the sea / low | — | 254,778 | 67 calls |

Frame time is vsync-bound on this host and unchanged within noise; the harbor adds about 24 calls and 21k triangles to a view that also holds the Haven, lighthouse and Moonbloom grove. The loaded cache reports four URLs, seven leases, ten shared images, **15,326,172 unique GLB bytes** and the unchanged **9,786,696** estimated decoded texture bytes.

Live checks: walking views of all three buildings from the square, cottage and tavern close-ups, the tavern interior (roof and near walls cut away, chest prompt reachable), the aerial glide-in over the harbor, low graphics (dune grass cells thin to 42% and distant cells hide, architecture stays) and a clean console/network log. Reduced motion and both failure paths are exercised by `test/saltwind-harbor.test.js`: the wind uniform reads zero, a harbor-only failure keeps authored Watch/Farm/Market plus the complete harbor fallback including the original work-site batch, and a shared-library failure releases the separately arriving harbor source. Regeneration reproduced the shipping GLB byte for byte (`26b6fb7343ded602850328ee40a12fd075be7526382ab6e1f51d2f86b69bff51`; manifest `10091588c0f039c8a70663d09591b843d1ad6b6a9e19e2a8a8e955b980fff664`), and the farm's prop-site hash and the scenery RNG-stream regression still pass, so rerouting the harbor's work sites and lawn tufts changed no original geometry or random draws. Full `npm.cmd test` passed **161 tests**; `git diff --check` passed.

Visual review history, kept so the next area does not repeat it: the first in-engine pass rendered the net-house near black although the Cycles preview looked fine — the runtime binding loop was not applying `normalScale`, and wall multipliers of 1.5–2 on the dark shared timber were far too timid; a side-by-side against the installed Tideglass cottage wall set the 5–10 range now recorded above. The painted roofs then read grey-mauve until red was pushed near 10 against the slate base. Hung details (sign, lantern, buoys) were first buried inside the siding thickness and were moved proud of the boards.

## Driftwood Yard and Sunwake Strand milestone QA — 2026-09-06

Baseline `ae4c379` was served from the local Docker deployment; the final source ran from the working tree on an isolated Node server (`PORT=3492`, separate `DATA_DIR`) and then from Docker at `668ec8c`. Both were driven through the fixed-render fixture of the Tideglass section (Chrome/WebGL2, 1920 × 1080, device pixel ratio 1, five-player snapshot, 300 frames per view with the first 60 discarded), reproduced by `.qa/driftwood-capture.mjs`-style scripts; the instructions here are enough to recreate it. Yaw is `atan2(-(tx - x), -(tz - z))` so the third-person camera looks from the player at a target.

| Walking / close view | Player X, Z | Looks toward |
| --- | --- | --- |
| Yard square / performance | 30, 99 | 36, 102 (shed) |
| Shed front | 29.4, 95.4 | 36, 102 |
| Cottage front | 30.5, 94 | 33, 85 |
| Hull frame | 36, 96 | 41.7, 97.6 |
| Timber stack | 31, 92 | 27.6, 87.6 |
| Shed / cottage interior | 36, 102 / 33, 85 | rear doorway, pitch -.13 |
| Strand landing / performance | 0, 92 | 0, 120 |
| Pier | 0, 112 | 0, 127 |
| Banner line | 2, 108 | 0, 101 |
| Landing crate | 8, 96 | 13, 101 |
| Harbor comparison / performance | -22.9, 96.5 | yaw -.36, pitch -.2 |

The yard aerial sets the camera at `(10, 34, 70)` looking at `(33, 3, 96)`; the strand aerial at `(-2, 30, 142)` looking at `(6, 3, 100)`; the glider approach uses player `(10, 22, 60)` gliding toward `(0, 100)` at pitch -.35.

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Baseline → final mean / p95 |
| --- | ---: | ---: | ---: |
| Yard / high | 118 → 163 | 309,015 → 335,097 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Yard / low | 117 → 162 | 308,855 → 326,665 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Strand / high | 174 → 223 | 348,494 → 391,815 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Strand / low | 173 → 222 | 331,038 → 354,055 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Harbor / high | 618 → 627 | 764,106 → 786,773 | 16.67 / 16.8ms → 16.67 / 16.8–16.9ms |
| Harbor / low | 590 → 599 | 624,750 → 634,633 | 16.67 / 16.8ms → 16.67 / 16.8ms |

Every sample is vsync-bound on this host; the kit adds about 45–50 calls and 26–43k renderer triangles to the yard and strand views and under 10 calls to the harbor. The loaded cache reports five URLs, nine leases, ten shared images, **17,758,520 unique GLB bytes** and the unchanged **9,786,696** estimated decoded texture bytes. The final geometry adds **2,432,348 bytes**, **26,569 source triangles** and **82 material primitives** with zero texture images; GLB SHA-256 `5d73fa9ff03170eff23ecb06d1085c59e1ee26e82c6948adb3ab7edea955290b`, manifest `1d6a7fffbd227e025ce45d1530ab9d2cea0ad4c3d7193e56b3e96959c939fd0a`, both reproduced byte for byte by an independent background Blender 5.2.1 LTS run. The harbor, market, farm and Old Watch assets are unchanged.

Reviewed views: the yard square, shed front, cottage front, hull, timber stack, both interiors (roof and camera-facing walls cut away, furnishings and chests visible), the strand landing, pier, banner line, landing crate, the harbor comparison, both aerials and the glider approach, all with a clean console and network log and the kit reporting `ready`. Reduced motion, low-quality grass thinning and hiding, pennant double-siding, both asset-failure paths, exact-once disposal, route walkability, overlay planarity and the scenery RNG stream are covered by `test/driftwood-yard.test.js` and `test/environment.test.js`. Full `npm.cmd test` passed **177 tests** on the milestone tree and **189** after rebasing onto the concurrent loot commit; `git diff --check` passed.

Visual review history, kept so the next area does not repeat it: the first export came in at 4.9MB / 52.8k triangles and was slimmed before shipping (see the cost section); the first in-engine pass rendered the cottage shingles as blue-grey slate and its clinker as orange brick, fixed by pulling shingle blue down to 4.3 and deepening the oxblood tint to 3.6/1.05/.9; the half-hull sign, oars and tool rack had to be re-sited to satisfy the .16m hung-detail bound. The signpost's first two candidates fell inside the 9m drop-point clearance, so it stands at `(-4.5, 104)`. Known limitations: the pier's last two sections clamp level at 1.15m, so the seaward deck sits about a metre above the water like the original planks' .75m floor; this milestone's populated view is the five-player fixture rather than a live session; the retained harbor rock and skiffs are still original scenery.

## Palmheart Camp and Wilds milestone QA — 2026-09-07

Baseline `b49cfb2` (environment `668ec8c`) was served from the local Docker deployment; the final source ran from the working tree on an isolated Node server (`PORT=3493`, separate `DATA_DIR`). Both were driven through the fixed-render fixture of the Tideglass section (Chrome/WebGL2, 1920 × 1080, device pixel ratio 1, five-player snapshot, 300 frames per view with the first 60 discarded), reproduced by a `.qa/palmheart-capture.mjs`-style script; the instructions here are enough to recreate it. Yaw is `atan2(-(tx - x), -(tz - z))` so the third-person camera looks from the player at a target.

| Walking / close view | Player X, Z | Looks toward |
| --- | --- | --- |
| Camp square / performance | -87, 9 | -92, -1 (tent) |
| Tent front / side | -90.2, 4.2 / -97, 1 | -92, -1 |
| Fire ring | -92.5, 6.5 | -96, 3 |
| Gear rack | -91, 9.5 | -95, 13 |
| Lantern | -83.5, 9.5 | -87.1, 13.1 |
| Chest corner | -90, 11 | -94, 9 |
| Shrine approach / performance | -76, 15 | -88, 8 |
| Trail mid / performance | -79, 14.5 | -90, 6 |
| Wilds edge | -96, 24 | -90, 8 |
| Old Watch comparison / performance | -64, -76 | yaw .60, pitch .03 |

The camp aerial sets the camera at `(-66, 34, 34)` looking at `(-90, 5, 6)`; the wilds aerial at `(-48, 44, 40)` looking at `(-84, 4, 10)`; the glider approach uses player `(-66, 22, 34)` gliding toward `(-90, 6)` at pitch -.35.

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Baseline → final mean / p95 |
| --- | ---: | ---: | ---: |
| Camp / high | 164 → 220 | 675,908 → 1,045,844 | 17.57 / 18.2ms → 17.59 / 18.4ms |
| Camp / low | 161 → 217 | 411,132 → 664,548 | 17.55 / 18.2ms → 17.58 / 18.2ms |
| Trail / high | 108 → 180 | 566,512 → 968,887 | 17.59 / 18.3ms → 17.56 / 18.1ms |
| Trail / low | 105 → 177 | 301,736 → 574,933 | 17.53 / 18.2ms → 17.54 / 18.2ms |
| Shrine approach / high | 113 → 186 | 519,760 → 925,159 | 17.61 / 18.4ms → 17.52 / 18.3ms |
| Shrine approach / low | 111 → 184 | 309,416 → 583,621 | 17.55 / 18.2ms → 17.57 / 18.3ms |
| Old Watch / high | 150 → 150 | 658,274 → 655,582 | 17.55 / 18.2ms → 17.60 / 18.4ms |
| Old Watch / low | 149 → 149 | 488,226 → 485,534 | 17.55 / 18.2ms → 17.62 / 18.3ms |

Every sample is vsync-bound on this host and unchanged within noise (an earlier baseline run of the same script measured 16.67 / 16.8ms in every view, including Old Watch, so the .9ms spread is host scheduling, not the kit). The kit adds about 55–75 calls and 350–450k renderer triangles to the camp and trail views: the first pass measured 254 camp calls with 16 trees in 32m cells and a fire point light, and the shipped 220 comes from 64m tree cells, no light and 28 trees. Single walking views rose from 104 → 160 (camp square), 37 → 80 (fire ring), 42 → 87 (lantern), 53 → 126 (shrine approach), 121 → 194 (camp aerial) and 181 → 254 (glider approach) calls; the low-quality camp view rose 101 → 157 with the ground cells thinned to 42%. The loaded cache reports six URLs, eleven leases, ten shared images, **20,551,472 unique GLB bytes** and the unchanged **9,786,696** estimated decoded texture bytes. The final geometry adds **2,792,952 bytes**, **28,831 source triangles** and **38 material primitives** with zero texture images; GLB SHA-256 `b11cb2b701f88e323cc9304f0f5abe79f4c0937a6e15b308c59efc8b042c42d5`, manifest `773400808d100a630f4a7743cdfed458640f63884dcb8f5a752b725a2da7186a`, both reproduced byte for byte by an independent background Blender 5.2.1 LTS run. The five earlier kits are unchanged.

Reviewed views: the camp square, tent front and side, fire ring, gear rack, lantern, chest corner, the shrine approach, the trail, the wilds edge, the Old Watch comparison, both aerials, the glider approach and the low-quality camp, all with a clean console and network log and the kit reporting `ready`. Browser failure checks (Playwright route abort, fresh contexts): blocking `/assets/palmheart-camp/kit.glb` left the five coastal kits `ready`, restored the original tent, work sites, coral flag and jungle vegetation and the baseline sky `85d9ee` at the camp; blocking `/assets/old-watch/kit.glb` restored every area's fallback; the unblocked run installed all six kits with the camp sky at `9fd5d2`. Reduced motion, low-quality thinning and hiding, shadow-casting canopy cells that never hide, the pennant hook, both asset-failure paths, exact-once disposal, route walkability, overlay planarity, the trunk-radius contract from the shipping GLB and the scenery RNG stream are covered by `test/palmheart-camp.test.js` and `test/environment.test.js`. Full `npm.cmd test` passed **212 tests**; `git diff --check` passed.

Implementation `ac1c874` was pushed to canonical `main` and deployed from a clean `git archive` export with `docker compose -p skywake-isles up -d --build --wait`. Compose reported healthy, `/health` returned `ok: true`, and 15 served assets — the changed client modules, the new shared module, the unchanged shared layout modules and all six kit GLBs — matched `git show HEAD:<path>` by SHA-256 after CRLF normalization. The persistent volume mount was preserved. The deployed camp square, tent side and camp aerial rendered with the kit `ready` and 20,551,472 unique GLB bytes reported, and the two failure checks repeated against the container with the same results. This evidence and the next bounded handoff are recorded in the rollout tracker.

Visual review history, kept so the next area does not repeat it: the first in-engine pass rendered the thatch as a saturated lime blanket (tint 2.4/2.1/1.0 with a green eave ramp) and the hardwoods as bare saplings with leaf tufts only at the branch tips, next to the chunky original canopy they replace; the fixes were a straw tint with red at 3.0, an olive-to-straw vertex ramp, and roughly doubled leaf clusters spread along the outer half of every limb. Planting with 2.2m clearance padding and 5.5m spacing saturated the area at 16 trees; 1.4m and 4m give 28. Only two original decorative trees fall inside the weight, so the authored planting sets the density rather than replacing a canopy one for one. Known limitations: the camp sits on the west-coast slope, so the sea shows behind the tent and the original chunky jungle trees remain visible beyond the 22m weight boundary (milestone 9 territory, like the harbor rock); the shrine's stone gateway and the pale boulder behind the tent are still original scenery; the populated view is the five-player fixture rather than a live crew.

## Cinderworks and Emberpeak milestone QA — 2026-09-07

Baseline `db145f4` (environment `ac1c874`) was served from the local Docker deployment; the final source ran from the working tree on an isolated Node server (`PORT=3494`, separate `DATA_DIR`). Both were driven through the fixed-render fixture of the Tideglass section (Chrome/WebGL2, 1920 × 1080, device pixel ratio 1, five-player snapshot, 300 frames per view with the first 60 discarded), reproduced by a `.qa/cinderworks-capture.mjs`-style script; the instructions here are enough to recreate it. Yaw is `atan2(-(tx - x), -(tz - z))` so the third-person camera looks from the player at a target.

| Walking / close view | Player X, Z | Looks toward |
| --- | --- | --- |
| Forge square / performance | 76, -44 | 83, -36 (forge) |
| Forge front / chimney side / back | 79.6, -41.2 / 90, -41 / 88, -30 | 83, -36 |
| Forge interior | 83, -36 | rear doorway 86.9, -31.4, pitch -.13 |
| Anvil | 74.5, -43.5 | 71.2, -41 |
| Ore pile | 80, -46.5 | 84, -44 |
| Lantern | 78.8, -38.2 | 82.1, -40.9 |
| Chest corner | 73.5, -36.5 | 77, -38 |
| Trail approach / performance | 60, -45 | 77, -43 |
| Outcrop | 74.5, -38 | 69, -34 |
| Caldera edge / performance | 62, -53 | 48, -65 (shrine) |
| Shrine to forge | 56, -57 | 77, -43 |
| Old Watch comparison / performance | -64, -76 | yaw .60, pitch .03 |

The forge aerial sets the camera at `(60, 32, -20)` looking at `(80, 3, -40)`; the area aerial at `(106, 40, -8)` looking at `(74, 3, -46)`; the glider approach uses player `(56, 22, -18)` gliding toward `(80, -42)` at pitch -.35.

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Baseline → final mean / p95 |
| --- | ---: | ---: | ---: |
| Forge / high | 155 → 226 | 305,734 → 377,374 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Forge / low | 154 → 225 | 305,574 → 353,270 | 16.67 / 16.8ms → 16.67 / 16.9ms |
| Trail / high | 113 → 193 | 294,430 → 376,318 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Trail / low | 112 → 192 | 294,270 → 345,510 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Caldera edge / high | 269 → 283 | 640,941 → 656,417 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Caldera edge / low | 256 → 270 | 599,793 → 607,685 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Old Watch / high | 150 → 150 | 655,582 → 655,012 | 16.67 / 16.8ms → 16.67 / 16.8ms |
| Old Watch / low | 149 → 149 | 485,534 → 484,964 | 16.67 / 16.8ms → 16.67 / 16.9ms |

Every sample is vsync-bound on this host and unchanged within noise. The kit adds about 70–80 calls and 50–80k renderer triangles to the forge and trail views and about 14 calls at the caldera edge, where only the apron's outer boulders are in frame. Single walking views rose from 101 → 172 (forge square), 112 → 168 (front), 224 → 305 (back), 80 → 118 (interior), 54 → 117 (ore pile), 53 → 114 (lantern), 48 → 108 (chest corner), 54 → 134 (trail approach), 102 → 185 (shrine to forge), 54 → 137 (forge aerial), 222 → 305 (area aerial) and 77 → 160 (glider approach) calls; the low-quality forge view rose 100 → 171 with the ground cells thinned to 42%. The loaded cache reports seven URLs, thirteen leases, ten shared images, **23,319,480 unique GLB bytes** and the unchanged **9,786,696** estimated decoded texture bytes. The final geometry adds **2,768,008 bytes**, **30,284 source triangles** and **56 material primitives** with zero texture images; GLB SHA-256 `543754ecf825f2896b2b0ce70f98fc92a75e568164883fca7ecafe54e801abea`, manifest `4454e094f37539c53f21ec05b786cbb8778ddb8b57ccf4e2bfd48b5bd5e902e9`, both reproduced byte for byte by an independent background Blender 5.2.1 LTS run. The six earlier kits are unchanged.

Reviewed views: the forge square, front, chimney side, back, interior cutaway (hood, flagstones, workbench, casks and chest-32), anvil, ore pile, lantern, chest corner, trail approach, outcrop, caldera edge, shrine-to-forge, the Old Watch comparison, both aerials, the glider approach and the low-quality forge, all with a clean console and network log and the kit reporting `ready`; the forge sky reads `cfccbb` while the shrine-to-forge view at `(56, -57)` keeps the baseline `85d9ee`. Browser failure checks (Playwright route abort, fresh contexts): blocking `/assets/cinderworks/kit.glb` left the six other kits `ready`, restored the original forge, work sites and Emberpeak scenery, kept the chimney smoke and the baseline sky at the square; blocking `/assets/old-watch/kit.glb` restored every area's fallback; the unblocked run installed all seven kits. Reduced motion, low-quality thinning and hiding, shadow-casting boulder cells that never hide, the sign and grass hooks, both asset-failure paths, exact-once disposal, route walkability through both doorways, overlay planarity, the doorway prisms, wall and roof envelopes, the chimney seat, the sign depth, the outcrop envelope and the scenery RNG stream are covered by `test/cinderworks.test.js` and `test/environment.test.js`. Full `npm.cmd test` passed **222 tests**; `git diff --check` passed.

Implementation `59f9661` was pushed to canonical `main` and deployed from a clean `git archive` export with `docker compose -p skywake-isles up -d --build --wait`. Compose reported healthy, `/health` returned `ok: true`, and 17 served assets — the changed client modules, the new shared module, the unchanged shared layout modules and all seven kit GLBs — matched `git show HEAD:<path>` by SHA-256 after CRLF normalization. The persistent volume mount was preserved. The deployed forge square, chimney side, interior and forge aerial rendered with the kit `ready` and 23,319,480 unique GLB bytes reported, and the two failure checks repeated against the container with the same results. This evidence and the next bounded handoff are recorded in the rollout tracker.

Visual review history, kept so the next area does not repeat it: the first in-engine pass rendered every basalt surface as a black silhouette (tint .70/.60/.58 over .45 vertex colours), the ash overlay as a hard dark-red disc from the air, and the sky as a flat grey overcast (`b4b7b1`); the fixes were basalt near 1.75 with the shared stone's green pulled down, soot near 1.0, boards at 1.15/.90/.72, an overlay that fades with the square of the weight and lighter ash greys, and a warmer, brighter profile (ambient 1.75, sun 2.4, peach sky at .72 strength). The first weight ramps (8m yard, 6m halo and exclusion) stepped the ambient by up to .0108 per .1m at the yard edge; 9m ramps bring the worst step anywhere to .0098. Masonry running to the doorway edge opened see-through joint slots in the reveal, so the courses stop at x ±1.35 and the wider jamb post owns the reveal, and a soot backing plane seals every joint. Known limitations: the forge sits on the east coast, so the sea and the Moonbloom grove show behind it and the original caldera crescent, ember shard and the two large original rocks north of the yard remain original scenery (milestone 9 territory); the interior flue hood and the chimney stack do not share an x, so a hidden .44m gap sits between them inside the roof volume; the populated view is the five-player fixture rather than a live crew.

## Moonwatch Observatory and Moonbloom Grove milestone QA — 2026-09-07

Baseline `d942e72` (environment `59f9661`) was served from the local Docker deployment; the final source ran from the working tree on an isolated Node server (`PORT=3495`, separate `DATA_DIR`). Both were driven through the fixed-render fixture of the Tideglass section (Chrome/WebGL2, 1920 × 1080, device pixel ratio 1, five-player snapshot, 300 frames per view with the first 60 discarded), reproduced by a `.qa/moonwatch-capture.mjs`-style script; the instructions here are enough to recreate it. Yaw is `atan2(-(tx - x), -(tz - z))` so the third-person camera looks from the player at a target.

| Walking / close view | Player X, Z | Looks toward |
| --- | --- | --- |
| Dome square / performance | 95.5, 41.8 | 104, 46 (dome) |
| Dome front / side / back | 98.5, 43.2 / 100.4, 53.2 / 111.2, 49.6 | 104, 46 |
| Telescope | 95.8, 36.3 | 93.47, 33.48 |
| Star table | 94.5, 46.2 | 97.88, 48.31 |
| Chart crate | 90.8, 42.8 | 88, 45 |
| Lantern | 96.2, 42.6 | 98.6, 45 |
| Chest corner (chest-25) | 89.5, 49.5 | 92, 47 |
| Trail approach | 89, 36.5 | 104, 46 |
| Trail / performance sample | 84, 34.5 | 96, 42 |
| Grove edge / performance (shrine side) | 80, 30 | 104, 46 |
| Silver tree (dressed `prop-28`) | 104, 40 | 109, 35 |
| Moon boulder (dressed `prop-33`) | 104.5, 54 | 109, 57 |
| Old Watch comparison / performance | -64, -76 | yaw .60, pitch .03 |

The dome aerial sets the camera at `(80, 32, 24)` looking at `(100, 8, 44)`; the area aerial at `(128, 42, 72)` looking at `(96, 6, 42)`; the glider approach uses player `(72, 22, 18)` gliding toward `(104, 46)` at pitch -.35.

| View / quality | Draw calls, baseline → final | Renderer triangles, baseline → final | Baseline → final mean / p95 |
| --- | ---: | ---: | ---: |
| Dome / high | 100 → 165 | 294,128 → 526,426 | 17.58 / 18.1ms → 16.67 / 16.8ms |
| Dome / low | 99 → 164 | 293,968 → 445,650 | 17.57 / 18.2ms → 16.67 / 16.8ms |
| Trail / high | 119 → 203 | 304,436 → 558,296 | 17.55 / 18.1ms → 16.67 / 16.8ms |
| Trail / low | 118 → 202 | 304,276 → 465,560 | 17.53 / 18.2ms → 16.67 / 16.8ms |
| Grove edge / high | 124 → 208 | 304,652 → 558,512 | 17.59 / 18.1ms → 16.67 / 16.8ms |
| Grove edge / low | 123 → 207 | 304,492 → 465,776 | 17.56 / 18.2ms → 16.67 / 16.8ms |
| Old Watch / high | 150 → 150 | 655,012 → 651,380 | 17.60 / 18.2ms → 16.67 / 16.8ms |
| Old Watch / low | 149 → 149 | 484,964 → 481,332 | 17.59 / 18.3ms → 16.67 / 16.8ms |

Every sample is vsync-bound on this host; the baseline run happened to land at 17.5–17.6ms and the final at 16.67ms in every view including Old Watch, so the .9ms spread is host scheduling, not the kit. The kit adds about 65–85 calls and 230–260k renderer triangles to the dome, trail and grove-edge fixtures — 22 shadow-casting trees and 24 shadow-casting mushrooms are the bulk of it — and nothing to Old Watch. Single walking views rose from 42 → 107 (dome square), 37 → 96 (dome front), 125 → 196 (dome side), 576 → 659 (dome back), 465 → 492 (telescope), 41 → 101 (star table), 530 → 558 (chart crate), 42 → 103 (lantern), 54 → 135 (chest corner), 44 → 126 (trail approach), 62 → 146 (grove edge), 22 → 59 (silver tree), 28 → 58 (moon boulder), 48 → 134 (dome aerial), 350 → 438 (area aerial) and 83 → 170 (glider approach) calls; the low-quality dome view rose 41 → 106 with the ground cells thinned to 42%. The loaded cache reports eight URLs, fifteen leases, ten shared images, **25,763,660 unique GLB bytes** and the unchanged **9,786,696** estimated decoded texture bytes. The final geometry adds **2,444,180 bytes**, **26,784 source triangles** and **43 material primitives** with zero texture images; GLB SHA-256 `3453b31b0abba0d57e91ff45b29382278b1b722afbafe4e14ab90a03bc8d78f7`, manifest `b9d45d3a01285d3b03ffafa82e3f024d3f8c31495a9763bdf3cc2792e31a4bad`, both reproduced byte for byte by two independent background Blender 5.2.1 LTS runs; the tint revision changed only the manifest, leaving the GLB hash identical to the first export. The seven earlier kits are unchanged.

Reviewed views: the dome square, front, side and back, the telescope, star table, chart crate, lantern, chest corner, trail approach, grove edge, the dressed silver tree and moon boulder, the Old Watch comparison, both aerials, the glider approach and the low-quality dome, all with a clean console and network log and the kit reporting `ready`; the dome square sky reads `b6cbe9` and the dome front `b8cbe8`, while the trail approach at `(89, 36.5)`, the grove edge at `(80, 30)` and the glider start keep the baseline `85d9ee`. Browser failure checks (Playwright route abort, fresh contexts): blocking `/assets/moonwatch/kit.glb` left the seven other kits `ready`, put Moonwatch in `fallback` ("Failed to fetch") with `moonwatch-original-exterior` and `moonwatch-original-scenery` visible, no authored group, the baseline sky and 44 calls at the dome square, and only the deliberately aborted request failing; blocking `/assets/old-watch/kit.glb` restored every area's fallback with both Moonwatch fallbacks visible, the baseline sky and no kit URLs loaded; the unblocked run installed all eight kits with both fallbacks hidden, the authored group visible, sky `b6cbe9` and 107 calls. Weight and lighting continuity, overlay planarity and the dome-footprint skip, atomic installation, lease preservation and frozen bindings, the failed/late/rollback load paths, missing prefab, material and site failures, the shipping GLB's contracts and `COLOR_0` averages and the scenery RNG stream are covered by `test/moonwatch.test.js` and `test/environment.test.js`. Full `npm.cmd test` passed **239 tests**, 11 of them the new `test/moonwatch.test.js`; `git diff --check` passed.

Visual review history, kept so the next area does not repeat it: the first in-engine pass failed on five tints at once — lavender leaves at 1.55/.95/1.85 read as dead olive over brown 2.6/2.5/2.9 bark, the 4.8/5.0/5.6 staves as warm tan indistinguishable from the ashlar, the 2.2/1.6/3.2 caps on `ground_earth` as dark plum and the 2.4/2.1/2.9 moonbells on `needle_foliage` as green — and the fix was tint-only (blue in the 8–20 range, green well below red, two slots moved onto `watch_stone`, lilac overlay colours), so the GLB never changed. The first "after" frame-time run overlapped a concurrent `npm.cmd test` and read 83–85ms mean in the Moonwatch views against 16.67ms at Old Watch, sampled after the tests ended; rerun alone it is the table above, so a contended capture must be discarded rather than interpreted. Every scholar-bench candidate anchored on the telescope fell inside the guard-ring or door-approach clearance, so the bench was re-anchored to the star table and stands at `(95.48, 45.51)` with the armillary sphere at `(94.28, 50.11)`. Known limitations: the telescope and chart-crate corner sit at about a third of the weight — faint moss, two thirds of the profile light — as a deliberate feather toward the shrine; the silver trees stand 8.57 and 10.65m against their nominal 9 and 11m, so the dressed collidable tree renders about 3% under its collider height; `moon_boulder` is 212 triangles and reads faceted up close; the shrine set (four large mushrooms, crystal ring and gate ring), the original silver trees beyond the 23m weight and `prop-29` remain original scenery; the populated view is the five-player fixture rather than a live crew.

## Extending the next area

1. Pick a small playable composition and specific reference characteristics: stone color, timber age, roof shape, vegetation and light. Establish collider and doorway envelopes before authoring.
2. Write the palette card and check it against the [Area identity contract](#area-identity-contract): siding construction, trim, roof colour, accents, ground and light must all differ from every shipped row, and neighbouring buildings inside the area must differ from each other. Keep the beach and coast lighter than the northern ruins.
3. Reuse these material families where the surface language matches. Add a new small texture set only for a distinct surface, and record its origin and license in `CREDITS.md`.
4. Add a ground-centered prefab with explicit UVs, vertex wear and an intentional silhouette. Merge by material. Add its naming, bounds and collision contract to both the generator validation and runtime integration.
5. Export geometry additions into a new area's asset directory with declared shared material dependencies, integrate with visible fallback, and compare gameplay screenshots and renderer statistics at the same camera/quality settings. Preserve broad paths and readable interactive objects. Add profiles to the one lighting owner, not another area-specific global light writer.
6. Commit the reproducible source, final GLB and manifest together after visual and gameplay checks. Reuse that validated pattern for a larger area only after the sample looks and performs well.

See [ENVIRONMENT_ROLLOUT.md](ENVIRONMENT_ROLLOUT.md) for the authoritative milestone order, completion evidence, active handoff and the **"upgrade next zone"** resume workflow. Keep progress there so fresh windows can continue without previous chat history.

This milestone establishes a reusable pipeline, with a first shared material/prop owner rather than a finished island-wide art library. Geometry and texture patterns are procedurally authored in the source generator; there is no hand-sculpted high-poly bake, painted unique facade atlas, terrain displacement, baked global illumination, seasonal art set or native-engine virtualized geometry. Those are possible later investments if the playable sample justifies them. Extra polygon count alone will not improve the result: composition, surface scale, edge wear and light need to be reviewed together.
